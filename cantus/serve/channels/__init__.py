"""cantus.serve.channels — webhook channel adapters (v0.4.5).

LINE and Telegram inbound HTTP webhook receivers + outbound reply clients.
Each adapter conforms to :class:`cantus.serve.channel.WebhookChannel`.
"""

from __future__ import annotations
