## 1. 依賴與設定

- [x] 1.1 為 Requirement「serve extras adds pynacl and websockets dependencies」呼應 Decision「Decision 3: Ed25519 驗章用 PyNaCl，不用 cryptography」與「Decision 4: WebSocket client 用 `websockets`，不用 `aiohttp.ws` 或 `httpx-ws`」，在 `pyproject.toml [project.optional-dependencies] serve` 加入 `pynacl>=1.5,<2` 與 `websockets>=13`；驗證 `uv pip compile --extra serve` 解出 lock 且 `[tool.uv].conflicts` 未增條目；驗證 `uv pip install -e '.[serve]'` 於 Linux/macOS/Windows 三 OS smoke 全綠（CI 工作流會跑）。
- [x] 1.2 [P] 為 Requirement「Settings adds three Discord channel fields with masked secrets」在 `cantus/config.py Settings` 新增 `channel_discord_bot_token: SecretStr | None = None`、`channel_discord_public_key: SecretStr | None = None`、`channel_discord_application_id: str | None = None` 並掛 env binding `CANTUS_SERVE_CHANNEL_DISCORD_*`；驗證 `pytest tests/serve/test_config.py::test_discord_fields_load_from_env -v` 全綠。
- [x] 1.3 [P] 為 Requirement「Settings adds three Discord channel fields with masked secrets」補測試確認兩個 SecretStr 欄位在 `repr(settings)` 與 `settings.model_dump_json()` 被 mask、`channel_discord_application_id` 明文保留（含 OpenAPI schema 不外洩 SecretStr 但保留 application_id）；驗證 `pytest tests/serve/test_config.py::test_discord_secrets_masked -v` 全綠。

## 2. RealtimeChannel Protocol

- [x] 2.1 為 Requirement「RealtimeChannel Protocol extends Channel with connect/disconnect lifecycle」呼應 Decision「Decision 1: RealtimeChannel 作為 WebhookChannel 的 sibling sub-Protocol（不是 child / parent）」，在 `cantus/serve/channel.py` 末尾新增 `RealtimeChannel(Channel, Protocol)`、`@runtime_checkable`、宣告 `async def connect(self) -> None` 與 `async def disconnect(self) -> None`；驗證 `mypy --strict cantus/serve/channel.py` clean 且 `python -c "from cantus.serve.channel import RealtimeChannel; print(RealtimeChannel)"` 印 class 物件。
- [x] 2.2 [P] 為 Requirement「RealtimeChannel Protocol extends Channel with connect/disconnect lifecycle」呼應 Decision「Decision 1: RealtimeChannel 作為 WebhookChannel 的 sibling sub-Protocol（不是 child / parent）」補三條測試案例（驗證 Protocol sibling 獨立性）：實作 connect/disconnect 的 stub class 走 `isinstance(_, RealtimeChannel)` 為 True、`LocalMockReceiver` 為 False、`LineWebhookChannel` 為 False；驗證 `pytest tests/serve/test_channel.py::test_realtime_channel_membership -v` 全綠。
- [x] 2.3 [P] 為 Requirement「RealtimeChannel Protocol extends Channel with connect/disconnect lifecycle」確認 `RealtimeChannel` 出現在 `cantus.serve.__all__` 且 `from cantus.serve import RealtimeChannel` 成功；驗證 `pytest tests/serve/test_init_exports.py::test_realtime_channel_exported -v` 全綠。

## 3. Ed25519 簽章模組與例外

- [x] 3.1 為 Requirement「DiscordSignatureError exists and never leaks secrets」與 Requirement「Discord Interactions HTTP endpoint verifies Ed25519 signatures and returns indistinguishable 401」呼應 Decision「Decision 3: Ed25519 驗章用 PyNaCl，不用 cryptography」，建立 `cantus/serve/channels/_ed25519.py` 匯出 `verify_ed25519(public_key_bytes: bytes, message: bytes, signature: bytes) -> None` 與常數 `SIGNATURE_HEADER = "X-Signature-Ed25519"`、`TIMESTAMP_HEADER = "X-Signature-Timestamp"`；驗證失敗 raise `DiscordSignatureError`；驗證 `mypy --strict cantus/serve/channels/_ed25519.py` clean。
- [x] 3.2 [P] 為 Requirement「DiscordSignatureError exists and never leaks secrets」在 `cantus/serve/channels/discord.py`（或拆於 `_errors_discord.py` 視組織）定義 `DiscordSignatureError(Exception)`、constructor 只有 `message: str` 一個參數、預設 `"discord interaction signature verification failed"`；驗證 `pytest tests/serve/channels/test_ed25519.py::test_signature_error_fixed_message -v` 全綠（含 `inspect.signature` 檢查只有一個參數）。
- [x] 3.3 [P] 為 Requirement「Discord Interactions HTTP endpoint verifies Ed25519 signatures and returns indistinguishable 401」建立 `tests/serve/channels/test_ed25519.py` 覆蓋四場景：合法簽章 verify 通過 / tampered body raise `DiscordSignatureError` / tampered timestamp raise / signature 長度錯 raise；驗證 `pytest tests/serve/channels/test_ed25519.py -v` 全綠。

## 4. WebSocket Gateway 內部模組

- [x] 4.1 為 Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」呼應 Decision「Decision 4: WebSocket client 用 `websockets`，不用 `aiohttp.ws` 或 `httpx-ws`」與「Decision 5: WS 連線作為 lifespan 啟動的 asyncio.Task，不開新 thread」與「Decision 7: 預設 intents = GUILDS | GUILD_MESSAGES | MESSAGE_CONTENT | DIRECT_MESSAGES」，建立 `cantus/serve/channels/_realtime.py` 內部 class `GatewayClient`：連線 `wss://gateway.discord.gg/?v=10&encoding=json`、收 op10 HELLO 起 heartbeat 迴圈、送 op2 IDENTIFY 帶 intents、追 op11 HEARTBEAT ACK；驗證 `mypy --strict cantus/serve/channels/_realtime.py` clean。
- [x] 4.2 為 Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」呼應 Decision「Decision 6: 連線復原走 RESUME-first、fall-back IDENTIFY 的退避策略」在 `_realtime.py` 加 RESUME 邏輯：保留 `session_id` 與最新 `seq`，斷線時優先送 op6 RESUME；Discord 回 op9 INVALID_SESSION 且 `d == false` 則清除 session 並走 fresh IDENTIFY；驗證 `pytest tests/serve/channels/test_realtime_gateway.py::test_resume_fallback_to_identify -v` 全綠。
- [x] 4.3 為 Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」呼應 Decision「Decision 6: 連線復原走 RESUME-first、fall-back IDENTIFY 的退避策略」在 `_realtime.py` 加 10 連敗停手紀律：累計 10 次 IDENTIFY rejection 後 set `self.last_error` 並退出 reconnect loop（**不** raise，避免拖垮 lifespan）；驗證 `pytest tests/serve/channels/test_realtime_gateway.py::test_ten_failures_stop_without_raise -v` 全綠。
- [x] 4.4 [P] 為 Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」呼應 Decision「Decision 4: WebSocket client 用 `websockets`，不用 `aiohttp.ws` 或 `httpx-ws`」與「Decision 7: 預設 intents = GUILDS | GUILD_MESSAGES | MESSAGE_CONTENT | DIRECT_MESSAGES」，建立 `tests/serve/channels/test_realtime_gateway.py` 並用 `websockets.serve` 起假 Discord Gateway 驗 IDENTIFY 流程（op10 → op2 → op0 READY）；驗證 `pytest tests/serve/channels/test_realtime_gateway.py::test_identify_flow -v` 全綠。
- [x] 4.5 [P] 為 Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」呼應 Decision「Decision 6: 連線復原走 RESUME-first、fall-back IDENTIFY 的退避策略」在 `test_realtime_gateway.py` 加 reconnect backoff 測試：連 3 次失敗、verify wait 序列 `[1s, 2s, 4s]`（monkeypatch sleep collector）；驗證 `pytest tests/serve/channels/test_realtime_gateway.py::test_exponential_backoff -v` 全綠。
- [x] 4.6 [P] 為 Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」在 `test_realtime_gateway.py` 加 heartbeat ACK miss 測試：模擬 heartbeat 未被 ACK，verify 連線 close code 1011 並嘗試 RESUME；驗證 `pytest tests/serve/channels/test_realtime_gateway.py::test_heartbeat_ack_miss -v` 全綠。

## 5. DiscordRealtimeChannel adapter

- [x] 5.1 為 Requirement「DiscordRealtimeChannel implements both RealtimeChannel and WebhookChannel」呼應 Decision「Decision 2: DiscordRealtimeChannel 同時實作 WebhookChannel + RealtimeChannel」與「Decision 10: application_id 為公開值，不用 SecretStr」，建立 `cantus/serve/channels/discord.py` 並宣告 `DiscordRealtimeChannel` 同時實作 `RealtimeChannel` 與 `WebhookChannel`；constructor 簽章 `bot_token / public_key / application_id / *, intents=DEFAULT_INTENTS, queue_maxlen=None, settings=None`；任一 secret 缺漏 raise `ValueError` 含子字串 `DiscordRealtimeChannel requires bot_token, public_key, and application_id` 且訊息不含 secret 值；驗證 `pytest tests/serve/channels/test_discord_construct.py -v` 全綠。
- [x] 5.2 為 Requirement「DiscordRealtimeChannel implements both RealtimeChannel and WebhookChannel」呼應 Decision「Decision 5: WS 連線作為 lifespan 啟動的 asyncio.Task，不開新 thread」實作 `connect()`：建立 `GatewayClient(bot_token, intents)` 並把 event callback 接到 internal `deque(maxlen=queue_maxlen)` 的 push；`disconnect()` 設 stop flag 並 close WS（close code 1000）；驗證 `pytest tests/serve/channels/test_discord_lifecycle.py::test_connect_disconnect_lifecycle -v` 全綠。
- [x] 5.3 為 Requirement「Discord Interactions HTTP endpoint verifies Ed25519 signatures and returns indistinguishable 401」呼應 Decision「Decision 8: Interactions HTTP 端點固定 `/channels/discord/interactions`，路徑命名空間沿用 v0.4.5 reserved `/channels`」實作 `mount(app)`：註冊 `POST /channels/discord/interactions`；handler 讀 raw body、組 `timestamp + body` 走 `verify_ed25519`；驗章失敗回 HTTP 401 body `{"detail":"Authentication required"}`；type 1 Ping 回 `{"type":1}` 且 **不** 入 queue；其他成功入 queue 回 `{"type":5}`；驗證 `pytest tests/serve/channels/test_discord_interactions.py -v` 含 Ping/應用指令/tampered body 401/missing header 401 四場景全綠。
- [x] 5.4 為 Requirement「send() routes by message dict shape — interaction callback or channel message」呼應 Decision「Decision 9: send() 出站路由依 message dict shape 自動 dispatch」實作 `send(message)` dispatch：`"interaction" in message` 走 interactions callback、`"channel_id" in message` 走 channels messages、皆缺則 raise `ValueError` 含子字串 `must carry 'interaction' or 'channel_id'`；驗證 `pytest tests/serve/channels/test_discord_send.py::test_send_routing -v` 全綠。
- [x] 5.5 為 Requirement「send() routes by message dict shape — interaction callback or channel message」呼應 Decision「Decision 9: send() 出站路由依 message dict shape 自動 dispatch」在 `send()` 加 4xx/5xx 處理：raise `ChannelSendError(status_code, body_excerpt=resp.text[:200], provider="discord")` 且 `str(err)` 不含 bot_token；驗證 `pytest tests/serve/channels/test_discord_send.py::test_send_error_no_token_leak -v` 用 obviously-fake token `"Bot-XYZ-secret"` 並斷言該字串不出現在 `str(err)`。
- [x] 5.6 [P] 為 Requirement「Discord Interactions HTTP endpoint verifies Ed25519 signatures and returns indistinguishable 401」用 FastAPI `TestClient` 驗證 401 body 對 tampered body 與 missing header 兩種情境 byte-identical；驗證 `pytest tests/serve/channels/test_discord_interactions.py::test_401_indistinguishable -v` 全綠。
- [x] 5.7 [P] 為 Requirement「DiscordRealtimeChannel implements both RealtimeChannel and WebhookChannel」與 Requirement「Settings adds three Discord channel fields with masked secrets」用 `respx` mock Discord REST API 並覆蓋 construct 矩陣：三 secret 全來自 Settings 建構成功 / constructor override 優先於 settings / application_id 缺漏 raise ValueError；驗證 `pytest tests/serve/channels/test_discord_construct.py -v` 全綠。

## 6. serve() lifespan 整合

- [x] 6.1 為 Requirement「serve() dispatches connect/disconnect to every RealtimeChannel via lifespan」呼應 Decision「Decision 5: WS 連線作為 lifespan 啟動的 asyncio.Task，不開新 thread」修改 `cantus/serve/app.py` 的 `serve()` lifespan：startup 過濾 `RealtimeChannel` 並 `asyncio.create_task(ch.connect())`、shutdown 對每個 RealtimeChannel `await ch.disconnect()` 後 cancel tasks，再 `await app.state.http_client.aclose()`；驗證 `mypy --strict cantus/serve/app.py` clean。
- [x] 6.2 [P] 為 Requirement「serve() dispatches connect/disconnect to every RealtimeChannel via lifespan」建立 `tests/serve/channels/test_realtime_lifecycle.py` 驗證：startup 對 `DiscordRealtimeChannel` 建立 task、shutdown 走 disconnect 再 close http_client、對 `LineWebhookChannel` 與 `LocalMockReceiver` 完全不呼叫 connect/disconnect；驗證 `pytest tests/serve/channels/test_realtime_lifecycle.py -v` 全綠。
- [x] 6.3 [P] 為 Requirement「serve() dispatches connect/disconnect to every RealtimeChannel via lifespan」呼應 Decision「Decision 1: RealtimeChannel 作為 WebhookChannel 的 sibling sub-Protocol（不是 child / parent）」與「Decision 2: DiscordRealtimeChannel 同時實作 WebhookChannel + RealtimeChannel」在 `test_realtime_lifecycle.py` 加 mixed channels 案例（`channels=[LocalMockReceiver, LineWebhookChannel, DiscordRealtimeChannel]`）：verify mount 走 Line+Discord、connect task 只建一次給 Discord、disconnect 同樣只給 Discord；驗證 `pytest tests/serve/channels/test_realtime_lifecycle.py::test_mixed_channels -v` 全綠。

## 7. 套件 re-exports、reserved path 與旁鄰測試

- [x] 7.1 為 Requirement「RealtimeChannel Protocol extends Channel with connect/disconnect lifecycle」與 Requirement「DiscordRealtimeChannel implements both RealtimeChannel and WebhookChannel」與 Requirement「DiscordSignatureError exists and never leaks secrets」修改 `cantus/serve/__init__.py` 加入三項 export 並排入 `__all__`；驗證 `pytest tests/serve/test_init_exports.py -v` 全綠。
- [x] 7.2 [P] 為 Requirement「DiscordRealtimeChannel implements both RealtimeChannel and WebhookChannel」與 Requirement「DiscordSignatureError exists and never leaks secrets」同步修改 `cantus/serve/channels/__init__.py` 加入 `DiscordRealtimeChannel`、`DiscordSignatureError` 並 re-export；驗證 `pytest tests/serve/channels/test_module_surface.py::test_discord_module_surface -v` 全綠。
- [x] 7.3 [P] 為 Requirement「/channels/discord/* reserved path namespace」呼應 Decision「Decision 8: Interactions HTTP 端點固定 `/channels/discord/interactions`，路徑命名空間沿用 v0.4.5 reserved `/channels`」在 `tests/serve/channels/test_discord_interactions.py` 加 route inspection 案例：`serve(..., channels=[DiscordRealtimeChannel(...)])` 後 `app.router.routes` 恰有一條 `path == "/channels/discord/interactions"` 且 method 為 POST；驗證 `pytest tests/serve/channels/test_discord_interactions.py::test_route_registered -v` 全綠。
- [x] 7.4 [P] 為 Requirement「/channels/discord/* reserved path namespace」在 `tests/serve/test_dashboard.py` 加旁鄰路徑案例確認 dashboard route 不被 `/channels/discord/interactions` 影響；驗證 `pytest tests/serve/test_dashboard.py -v` 全綠（含原有案例）。

## 8. 跨平台 wheel 紀律驗證

- [x] 8.1 為 Requirement「serve extras adds pynacl and websockets dependencies」在 `cantus/serve/channels/_realtime.py` 與 `cantus/serve/channels/_ed25519.py` 加 module-level docstring 列明支援平台矩陣（Linux x86_64 / macOS arm64+x86_64 / Windows AMD64 × CPython 3.10–3.13）；驗證 grep `"CPython 3.10"` 出現在兩檔 module docstring。
- [x] 8.2 [P] 為 Requirement「serve extras adds pynacl and websockets dependencies」在 `tests/serve/channels/test_discord_construct.py` 加 import-time smoke：`import nacl.signing` 與 `import websockets` 成功（不靠 source build）；驗證 `pytest tests/serve/channels/test_discord_construct.py::test_dependency_imports -v` 全綠於 CI 三 OS。

## 9. 文件與 governance

- [x] 9.1 為 Requirement「Cantus serve ships realtime channel gateway for Discord」撰寫 `docs/cookbook-discord-channel.md`：學生視角端到端 walkthrough — Discord Developer Portal 建 application/bot/public key、開啟 MESSAGE_CONTENT privileged intent、設 env vars、`cantus serve --channels`、bot 上線觀察心跳 log、手動註冊 slash command、Interactions Endpoint URL 設定（含 Cloudflare Tunnel 同 B1 cookbook 流程）；台灣繁中、可獨立執行；驗證 `markdownlint docs/cookbook-discord-channel.md` clean（或人工 review 通過）。
- [x] 9.2 [P] 為 Requirement「Cantus serve ships realtime channel gateway for Discord」撰寫 `MIGRATION_v0.4.5_to_v0.4.6.md`：對齊 B1 MIGRATION 結構，列 Breaking（無）、What's new（三新 public symbols + 三新 Settings + 兩新依賴 + lifespan 擴充 + reserved sub-path）、Upgrade command、What's NOT 與 why（Slack RTM / sharding / voice / slash auto-register / multi-bot 等 Non-Goals 條列）；台灣繁中；驗證人工 review 通過。
- [x] 9.3 [P] 為 Requirement「Cantus serve ships realtime channel gateway for Discord」在 `CHANGELOG.md` 開頭加 `## [0.4.6] - 2026-05-2x — cantus-channel-gateway-realtime` 條目，列 Added / Changed / Notes 三節並引用 MIGRATION 與 cookbook；版本 bump 留給 release runbook；驗證人工 review 通過。

## 10. 收尾：四維度驗證

- [x] 10.1 為 Requirement「Cantus serve ships realtime channel gateway for Discord」跑 `uv run pytest tests/serve -q` 整體全綠（v0.4.5 baseline 140 + 本件新增）；驗證輸出 `passed` 與 baseline 數一致。
- [x] 10.2 為 Requirement「DiscordRealtimeChannel implements both RealtimeChannel and WebhookChannel」與 Requirement「serve() dispatches connect/disconnect to every RealtimeChannel via lifespan」跑 `uv run mypy --strict cantus/serve/channels/` 與 `uv run mypy --strict cantus/serve/app.py cantus/serve/channel.py` 兩者皆 `Success: no issues found`。
- [x] 10.3 跑 `spectra analyze cantus-channel-gateway-realtime` 四維度 Clean（無 Critical / Warning）；驗證 JSON 輸出每個 dimension 的 `finding_count` 為 0。
- [x] 10.4 跑 `spectra validate cantus-channel-gateway-realtime` 印 ✓；驗證 exit code 0。
