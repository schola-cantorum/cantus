## 1. Translator 與 factory 擴充

- [x] 1.1 在 `libs/cantus/cantus/model/providers/_translate.py` 新增 `to_google_messages(messages) -> tuple[str | None, list[dict]]` 與 `from_google_response(raw) -> ChatResponse` 兩個純函式，落實設計決策「Google translator 為新增純函式 to_google_messages / from_google_response，不抽 base translator」：抽離 system message 為 top-level `system_instruction`、`assistant` → `model`、`tool` → `function` + `function_response` part、`function_call` part 翻回 `ToolCall`、`finish_reason` 映射 `STOP/MAX_TOKENS/SAFETY/TOOL_CALL` 至 cantus stop_reason、`usage_metadata` 翻回 `{"input_tokens", "output_tokens"}`。驗證：新增 `tests/providers/test_translate.py` 內五條測項涵蓋 system 抽離、role mapping table（user/assistant/system/tool）、tool_call round-trip、empty content、stop_reason 映射；`cd libs/cantus && uv run pytest tests/providers/test_translate.py -v -k google` 全綠
- [x] 1.2 在 `libs/cantus/cantus/model/factory.py` 把 `_REGISTRY` 擴為五條（追加 `google` / `groq` / `nvidia` 三 prefix），落實 spec「load_chat_model factory dispatches by provider prefix with lazy import」MODIFIED：`ValueError` 訊息列舉 `openai`、`anthropic`、`google`、`groq`、`nvidia` 五家；`nvidia` 的 missing-extras `ImportError` 提示 `pip install cantus[openai]`（特例），其他四家維持 `pip install cantus[<provider>]`。驗證：擴充 `tests/test_factory.py` 加入「未知 prefix 訊息列舉五家」「`google` missing-extras 提示 `cantus[google]`」「`groq` missing-extras 提示 `cantus[groq]`」「`nvidia` missing-extras 提示 `cantus[openai]` 且不含 `cantus[nvidia]`」「五家 prefix 各自 lazy import 對應 module」五條測項；`uv run pytest tests/test_factory.py -v` 全綠

## 2. 三家新 adapter 實作（複用既有 conftest 與 translator）

- [x] 2.1 `[P]` 在 `libs/cantus/cantus/model/providers/google.py` 實作 `GoogleChatModel`，落實 spec「GoogleChatModel adapter implements Tier 2 against google-genai SDK」與設計決策「Google adapter 採 google-genai 新 SDK 而非 google-generativeai 舊 SDK」：建構子接受 `model_id` / `api_key` / `**client_kwargs`；key 解析走 `resolve_api_key(api_key, "GOOGLE_API_KEY")`；lazy import `from google import genai`；`chat()` 透過 `to_google_messages()` 抽離 system 後呼叫 `client.models.generate_content(model=..., contents=..., system_instruction=...)`；`stream()` 透過 `client.models.generate_content_stream(...)` yield 純文字 delta；`supports_tool_use = True`。驗證：新增 `tests/providers/test_google_adapter.py` 含三條 VCR cassette（chat / chat+tools / stream）+ 一條 system 抽離行為測試 + 一條 `MissingAPIKeyError` 名 `GOOGLE_API_KEY` 測試 + 一條「在無 google-genai 安裝環境下顯式 import adapter 模組會拋 ImportError 而非 silent fallback 到 google-generativeai」測試；`uv run pytest tests/providers/test_google_adapter.py --record-mode=none` 全綠
- [x] 2.2 `[P]` 在 `libs/cantus/cantus/model/providers/groq.py` 實作 `GroqChatModel`，落實 spec「GroqChatModel adapter implements Tier 2 against the groq SDK」與設計決策「Groq adapter 透過 groq SDK 並複用 OpenAI 翻譯器」：建構子接受 `model_id` / `api_key` / `**client_kwargs`；key 解析走 `resolve_api_key(api_key, "GROQ_API_KEY")`；lazy import `from groq import Groq`；`chat()` / `stream()` 透過 `client.chat.completions.create(...)` 並**直接複用** `to_openai_messages` / `from_openai_response` 兩個既有純函式；`supports_tool_use = True`。驗證：新增 `tests/providers/test_groq_adapter.py` 含三條 VCR cassette（chat / chat+tools / stream）+ 一條 `MissingAPIKeyError` 名 `GROQ_API_KEY` 測試 + 一條「adapter 直接 import `to_openai_messages` / `from_openai_response`，無重複實作 Groq translator」靜態檢查（grep 或 import path assertion）；`uv run pytest tests/providers/test_groq_adapter.py --record-mode=none` 全綠
- [x] 2.3 `[P]` 在 `libs/cantus/cantus/model/providers/nvidia.py` 實作 `NvidiaChatModel`，落實 spec「NvidiaChatModel adapter is a thin subclass of OpenAIChatModel with hard-coded NIM base_url」與設計決策「NVIDIA adapter 為 OpenAIChatModel 的薄 subclass，固定 NIM base_url，不開獨立 extras」：subclass `OpenAIChatModel`；建構子預設 `base_url="https://integrate.api.nvidia.com/v1"`（caller 顯式傳入可覆寫）；key 解析改為 `resolve_api_key(api_key, "NVIDIA_API_KEY")`；不覆寫 `chat` / `stream` / `supports_tool_use`；實作 < 30 行。驗證：新增 `tests/providers/test_nvidia_adapter.py` 三條 VCR cassette（chat / chat+tools / stream，wire 與 OpenAI 等價）+ 一條「預設 `base_url` 為 NIM endpoint」測試 + 一條「顯式 `base_url` 覆寫」測試 + 一條 `MissingAPIKeyError` 名 `NVIDIA_API_KEY` 且訊息不指向 `OPENAI_API_KEY` 測試 + 一條「subclass 不覆寫 chat/stream」inspect.getsource grep 驗證；`uv run pytest tests/providers/test_nvidia_adapter.py --record-mode=none` 全綠

## 3. ARCH-2 邊界守護與 top-level export 政策

- [x] 3.1 落實 spec「Core import does not transitively load provider SDKs」MODIFIED 之保護範圍從 openai/anthropic 擴至 google/groq：擴充 `libs/cantus/tests/test_integration_smoke.py`，新增兩條測試 — (a) 在 `google-genai` 與 `groq` 已安裝的乾淨 Python process 內，`import cantus` 後 `sys.modules` 不含 `google.genai` / `groq`；(b) 顯式 `from cantus.model.providers.google import GoogleChatModel` 後 `google` 或 `google.genai` 出現在 `sys.modules`，顯式 `from cantus.model.providers.groq import GroqChatModel` 後 `groq` 出現在 `sys.modules`。驗證：`uv run pytest tests/test_integration_smoke.py -v` 全綠
- [x] 3.2 落實設計決策「Three new adapters opt out of top-level cantus re-export」：`libs/cantus/cantus/__init__.py` `__version__` 由 `"0.2.0"` bump 至 `"0.2.1"`；**不**把 `GoogleChatModel` / `GroqChatModel` / `NvidiaChatModel` 加入 top-level `__all__` 或 import 語句（v0.2.0 既有 export contract 保留）。驗證：`python -c "import cantus; assert cantus.__version__ == '0.2.1'; assert not hasattr(cantus, 'GoogleChatModel') and not hasattr(cantus, 'GroqChatModel') and not hasattr(cantus, 'NvidiaChatModel'); assert hasattr(cantus, 'load_chat_model') and hasattr(cantus, 'ChatModelAsHandle')"` 通過

## 4. pyproject extras 與發行物清點

- [x] 4.1 落實 spec「Distribution extras matrix exposes openai, anthropic, google, groq, providers, and dev groups」ADDED 與設計決策「Version bump 為 PATCH 0.2.0 → 0.2.1 而非 MINOR」：`libs/cantus/pyproject.toml` `version` 改為 `"0.2.1"`；`[project.optional-dependencies]` 新增 `google = ["google-genai>=0.3,<1"]`、`groq = ["groq>=0.11,<1"]`；`providers` 聚合器改為 `["cantus[openai,anthropic,google,groq]"]`；**不**新增 `nvidia` extras group；core `dependencies` 不變、不含 `litellm`、不含 `google-generativeai`。驗證：在乾淨 venv 下分別跑 `pip install -e ".[openai]"`（只裝 openai）、`pip install -e ".[google]"`（只裝 google-genai 不裝 google-generativeai）、`pip install -e ".[groq]"`（只裝 groq）、`pip install -e ".[providers]"`（四家齊裝、無 litellm、無 google-generativeai）、`pip install -e ".[nvidia]"`（pip 報 extras 不存在）、`pip install -e "."`（不裝任何 provider SDK）六條情境通過
- [x] 4.2 落實 spec「Distribution extras matrix exposes openai, anthropic, and providers groups」REMOVED 的 Migration 條件：確認 `libs/cantus/pyproject.toml` 與 README quickstart 不再以三家形式（openai / anthropic / providers）描述 extras matrix，全面改用五家形式（openai / anthropic / google / groq / providers）+ NVIDIA 走 `cantus[openai]` 的指引。驗證：`grep -n "openai.*anthropic.*providers" libs/cantus/README.md libs/cantus/README.zhTW.md libs/cantus/pyproject.toml libs/cantus/CHANGELOG.md` 結果中無「僅列三家」的舊描述；新版描述四家 extras + NVIDIA 指引各出現至少一次

## 5. Cassette 安全 hook 與 audit 範圍

- [x] 5.1 落實設計決策「擴充 cantus-distribution pre-push secret-pattern hook 涵蓋 cassette 路徑」與 spec「Pre-push security audit gates initial publication」MODIFIED 對 cassette 範圍的擴充：把 cantus-distribution pre-push hook（v0.2.0 archive 留下的 follow-up scope）的 secret-pattern grep 涵蓋 glob `libs/cantus/tests/providers/cassettes/**/*.yaml`，比對 patterns `Authorization:` / `Bearer ` / `x-api-key:` / `api-key:` / `x-goog-api-key:` / `sk-[A-Za-z0-9]{20,}` / `hf_[A-Za-z0-9]+` / `ghp_[A-Za-z0-9]+` / `AIza[A-Za-z0-9_-]{35}` / `AKIA[A-Z0-9]{16}`；任一非空 match 退出非零碼阻擋 push。驗證：手寫一條含 `Authorization: Bearer fake` 的 dummy cassette 餵 hook 應退出非零；移除該行後 hook 退出 0；`grep -rE 'Authorization:|Bearer |x-api-key:|api-key:|x-goog-api-key:' libs/cantus/tests/providers/cassettes/` 對實際錄製產物為空

## 6. 文件、CHANGELOG 與手動 smoke

- [x] 6.1 落實 README 雙語對照延續 v0.2.0 byte-identical 規則：在 `libs/cantus/README.md` 與 `libs/cantus/README.zhTW.md` 的 Multi-provider quickstart 區段為 Google / Groq / NVIDIA 三家各新增一個 7 行 code block（`load_chat_model("<provider>/<model>") + ChatModelAsHandle(...) + Agent(model=...)` 三步驟）；明示 NVIDIA 走 `cantus[openai]`；既有 OpenAI / Anthropic / Gemma quickstart 保留。驗證：`diff <(sed -n '/```python/,/```/p' libs/cantus/README.md) <(sed -n '/```python/,/```/p' libs/cantus/README.zhTW.md)` 顯示三個新 code block 與既有兩個 code block byte-identical
- [x] 6.2 `[P]` 落實 release-time 多 provider 手動回歸：新增 `libs/cantus/notebooks/multi_provider_smoke_batch2.ipynb`，Google / Groq / NVIDIA 三家各兩 cell（chat + stream），用 `python-dotenv` 讀 `.env` 不在 notebook 寫死 key；v0.2.0 既有 `multi_provider_smoke.ipynb` 不動。驗證：release manager release 前在本機跑全 notebook 無 error，三家各回非空字串並提交含 output 的版本
- [x] 6.3 落實設計決策「Version bump 為 PATCH 0.2.0 → 0.2.1 而非 MINOR」於 CHANGELOG：`libs/cantus/CHANGELOG.md` 新增 `## [0.2.1]` 條目，含 Added / Changed / Notes 三節；Added 列三家新 adapter 與 google / groq extras；Changed 列 factory `_REGISTRY` 擴 + `providers` 聚合器擴；Notes 節說明「NVIDIA 採 subclass 不開 extras」「Google 採 `google-genai` 新 SDK 不採 `google-generativeai` 舊 SDK」「Groq schema churn 鎖 `>=0.11,<1`」三條決策依據，並引用 v0.2.0 既有「不引入 LiteLLM」決策延續。驗證：`grep -A2 "## \[0.2.1\]" libs/cantus/CHANGELOG.md` 顯示三節標題；`grep -E "google-genai|不引入 LiteLLM|subclass" libs/cantus/CHANGELOG.md` 三段關鍵字皆命中
