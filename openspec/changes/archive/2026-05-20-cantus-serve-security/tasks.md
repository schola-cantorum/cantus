<!--
Every task states a behavior/contract delivered + a verification target.
File paths are locator context, not the task itself.
`[P]` markers per `.spectra.yaml: parallel_tasks: true`; tasks within the same
group marked `[P]` may run in parallel because they touch disjoint files.
-->

## 1. Config 與型別擴展

- [x] 1.1 [P] 在 `libs/cantus/cantus/config.py` 為 `Settings` 新增 `AuthMode(str, Enum)` 與四個欄位（`auth_mode: AuthMode = AuthMode.NONE`、`api_key: SecretStr | None = None`、`bearer_token: SecretStr | None = None`、`dashboard_requires_auth: bool = True`），交付的行為是「`CANTUS_SERVE_AUTH_MODE` / `CANTUS_SERVE_API_KEY` / `CANTUS_SERVE_BEARER_TOKEN` / `CANTUS_SERVE_DASHBOARD_REQUIRES_AUTH` 四個 env 變數可被 pydantic-settings 正確載入、enum value 採 `none` / `bearer` / `api-key`」。對應 Decision: AuthMode 用 str-valued enum 對應 pydantic-settings env 載入。驗證以 `pytest libs/cantus/tests/serve/test_config.py` 新增的 SecretStr env 載入 case 通過、`mypy --strict libs/cantus/cantus/config.py` 全綠。

## 2. Security 模組與閘口

- [x] 2.1 [P] 新增 `libs/cantus/cantus/serve/security.py`，實作 `require_auth()` FastAPI dependency 與 constant-time compare helper，交付的行為是「比對使用 `hmac.compare_digest(provided.encode(), expected.get_secret_value().encode())`；缺 / 錯 / 格式錯任一情況一律 raise `HTTPException(status_code=401, detail='Authentication required')` 不區分」。對應 Decision: Token 比對使用 hmac.compare_digest 與 Decision: 401 body 統一為 `{"detail": "Authentication required"}` 不區分缺/錯 token。驗證以 `pytest libs/cantus/tests/serve/test_security.py::test_bearer_missing_header_401`、`::test_bearer_wrong_token_401`、`::test_bearer_correct_token_200` 三個 case 通過，且三個 401 response body byte-identical。
- [x] 2.2 在 `libs/cantus/cantus/serve/app.py` 的 `serve()` 整合 `Depends(require_auth)`，交付的行為是「當 `settings.auth_mode != AuthMode.NONE` 時，`POST /skills/{name}` 必經 auth dependency；dashboard endpoints（`GET /skills` / `GET /health` / `GET /events`）依 `settings.dashboard_requires_auth` 決定是否套；當 `auth_mode != NONE` 但對應 token 欄位為 None 時，`serve()` 啟動即 raise `ValueError` fail-fast」。對應 Requirement: Cantus serve gates Skill endpoints behind opt-in authentication + Decision: auth_mode 預設 NONE 維持 v0.4.0 相容性 + Decision: dashboard 預設套 auth、可由 dashboard_requires_auth 關閉。驗證以 `pytest libs/cantus/tests/serve/test_security.py::test_none_mode_preserves_v040_behavior`、`::test_api_key_mode`、`::test_fail_fast_when_token_missing` 與 `libs/cantus/tests/serve/test_dashboard.py` 新增的 dashboard 401 case 通過。
- [x] 2.3 [P] 在 `libs/cantus/cantus/serve/__init__.py` re-export `AuthMode`、`require_auth` 兩個新公開 symbol，交付的行為是「`from cantus.serve import AuthMode, require_auth` 在安裝 `cantus[serve]` 後成功；既有 `serve` / `Channel` / `LocalMockReceiver` 三個 symbol 的 import 行為 byte-identical 保留」。驗證以 `pytest libs/cantus/tests/serve/test_lazy_import.py` 既有 6 case 全綠 + 新增一條 import case 確認 `AuthMode` 與 `require_auth` 可被 import。

## 3. 測試矩陣

- [x] 3.1 [P] 新增 `libs/cantus/tests/serve/test_security.py`，交付六 case 測試矩陣（NONE 預設無 auth、bearer 缺 header 401、bearer 錯 token 401、bearer 對 token 200、api-key 對 / 錯 / 缺三種對應、SecretStr 不洩漏於 `repr(settings)` / `settings.model_dump_json()` / `serve(registry).openapi()` 序列化結果 / `caplog.text`），對應 Requirement: Cantus serve gates Skill endpoints behind opt-in authentication. 驗證以 `pytest libs/cantus/tests/serve/test_security.py -v` 六 case 全綠 + SecretStr 不洩漏 case 內四條 `assert "correct-secret" not in ...` 全綠。
- [x] 3.2 [P] 在 `libs/cantus/tests/serve/test_config.py` 新增 SecretStr env 載入 case，交付的行為是「設定 `CANTUS_SERVE_BEARER_TOKEN=correct-secret` 後 `settings.bearer_token` 為 `SecretStr` instance、`settings.bearer_token.get_secret_value() == 'correct-secret'`、`repr(settings.bearer_token)` 不含 `correct-secret`」。驗證以 `pytest libs/cantus/tests/serve/test_config.py -v` 新 case 通過 + 既有 8 case byte-identical 保留。
- [x] 3.3 [P] 在 `libs/cantus/tests/serve/test_dashboard.py` 新增 `dashboard_requires_auth` 開關行為 case，交付的行為是「`auth_mode=bearer` + `dashboard_requires_auth=true`（預設）下 `GET /skills` / `GET /health` / `GET /events` 對匿名請求回 401；同樣配置改 `dashboard_requires_auth=false` 後三個 endpoint 對匿名請求回 200」。驗證以 `pytest libs/cantus/tests/serve/test_dashboard.py -v` 既有 9 case + 新增 2 case 全綠。

## 4. Packaging 與文件

- [x] 4.1 [P] 更新 `libs/cantus/pyproject.toml`：把 `[project].version` 從 `"0.4.0"` 改為 `"0.4.1"`；在 `[project.optional-dependencies]` 新增 `security = ["cantus[serve]"]`，交付的行為是「`pip install cantus[security]` 解析到的套件集合與 `pip install cantus[serve]` 完全相同（fastapi、uvicorn、pydantic-settings；無新套件、無新進 `[tool.uv] conflicts`）」。對應 Decision: cantus[security] extras 為 documentary alias、不引入新 dependency。驗證以 `uv pip install --dry-run 'cantus[security] @ file://libs/cantus'` 輸出與 `cantus[serve]` 對 diff 為空 + `[tool.uv] conflicts` 段保留既有 6 pairs + openhands `python_version >= "3.12"` marker byte-identical。
- [x] 4.2 [P] 更新 `libs/cantus/docs/protocols/serve.md`：新增「Authentication」段，交付的內容是「auth 三模式（none / bearer / api-key）說明 + 4 個新 `CANTUS_SERVE_*` env 變數對照表 + `dashboard_requires_auth` 行為 + 生產環境必改 `auth_mode` 醒目警示 + constant-time compare 與 401 不區分缺/錯的設計選擇備註」。驗證以人工 review 確認段落涵蓋上述五項 + 文件內所有 env 變數名稱與 `Settings` 欄位名稱一致。
- [x] 4.3 [P] 新增 `libs/cantus/MIGRATION_v0.4.0_to_v0.4.1.md`，交付的內容是「ADDITIVE 升級說明（auth_mode 預設 NONE 維持 v0.4.0 行為）+ 4 個新 env 變數列表 + bearer / api-key 兩種模式各一份範例 `.env` + Security note 醒目警告生產環境必改 + 預告 v0.4.2 tunnel helper 會把 auth_mode=NONE 視為警示 + 預告 v0.4.3 supply-chain CLI scope」。驗證以人工 review 確認段落涵蓋上述六項。
- [x] 4.4 [P] 更新 `libs/cantus/CHANGELOG.md`：新增 `## [0.4.1] - <date>` 段，交付的內容是「Keep a Changelog + SemVer 格式條列 Added（AuthMode / require_auth / 新 Settings 欄位 / security extras alias）/ Changed（serve() 整合 auth dependency）/ Security（SecretStr 包裝、constant-time compare、401 不區分）三類 + Deferred 段預告 v0.4.2 tunnel + v0.4.3 supply-chain CLI」。驗證以人工 review 確認段落涵蓋三類 + Deferred 預告。

## 5. 驗證與封口

- [x] 5.1 在 `libs/cantus/` 根目錄跑完整測試 + 型別 + coverage 驗證，交付的行為是「`pytest libs/cantus/tests/` 全綠（含新 test_security.py 六 case、test_config.py 新 case、test_dashboard.py 新 2 case）；`mypy --strict libs/cantus/cantus/` 全綠（含新 security.py、config.py 擴欄位、serve/app.py 與 __init__.py 變更）；coverage 報告新 `cantus/serve/security.py` 的 line + branch coverage 不低於既有 `cantus/serve/app.py` v0.4.0 baseline」。驗證以三個指令的 exit code 0 + coverage HTML 報告人工比對。
- [x] 5.2 跑 `/spectra-audit cantus-serve-security` 完整安全/品質稽核，交付的行為是「audit 完整覆蓋本 change 的 dangerous defaults / type confusion / silent failures / secret leak surface；重點稽核項目包含 SecretStr 在 repr/log/JSON/OpenAPI 四 surface 的不洩漏、constant-time compare 的 timing-oracle 防護、401 不區分缺/錯 token 的 enumeration 防護、auth_mode 預設 NONE 的安全性與 docs 警示完整性、新 4 個 env 變數命名與既有 `CANTUS_SERVE_*` 6 欄位無衝突、`[tool.uv] conflicts` 6 pairs 完整保留、dashboard_requires_auth 預設 True 取捨明文記錄」。對應 Decision: 本 change 範疇結束於 cantus 內 commit + tag v0.4.1的前置條件（audit 必須先全綠）。驗證以 `spectra audit cantus-serve-security` exit code 0 或所有 Critical / Warning finding 明文 accept；audit 報告人工 review。
- [x] 5.3 在 `libs/cantus/` 內完成 release 動作，交付的行為是「commit message 透過 `/tw-emoji-commit` skill 產出後執行 `git commit -F <tmpfile>`；`git tag v0.4.1` 後 `git push --tags`；release notes 透過 `/tw-emoji-release-note` skill 產出後 `gh release create v0.4.1 --notes-file <tmpfile>` 在 cantus 上游 repo 落地」。對應 Decision: 本 change 範疇結束於 cantus 內 commit + tag v0.4.1。驗證以 `git tag | grep v0.4.1` 出現新 tag、`gh release view v0.4.1 --repo schola-cantorum/cantus` 顯示 release notes 內容。
