## 1. Settings 加 channel-specific SecretStr 欄位

- [x] 1.1 [P] 為 Requirement「Settings exposes four channel-specific SecretStr fields」呼應 Decision「D5 Channel secrets live in Settings as SecretStr fields」在 `cantus/config.py` 為 `Settings` 新增四個欄位 `channel_line_secret`、`channel_line_access_token`、`channel_telegram_secret_token`、`channel_telegram_bot_token`，型別 `SecretStr | None`，預設 `None`，env 分別對應 `CANTUS_SERVE_CHANNEL_LINE_SECRET` / `CANTUS_SERVE_CHANNEL_LINE_ACCESS_TOKEN` / `CANTUS_SERVE_CHANNEL_TELEGRAM_SECRET_TOKEN` / `CANTUS_SERVE_CHANNEL_TELEGRAM_BOT_TOKEN`。驗證：`pytest tests/serve/test_config.py -k channel -v` 全綠，含 env 載入成功、未設預設 `None`、`SecretStr.get_secret_value()` 取回字面值、`repr(settings)` 與 `settings.model_dump_json()` 不出現任何明文 secret。
- [x] 1.2 [P] 為 Requirement「Settings exposes four channel-specific SecretStr fields」補測試確認四個新欄位不出現在 `app.openapi()` schema 任何位置（`generated_openapi_json["components"]["schemas"]` 與 settings-related 路徑掃過一遍）。驗證：`pytest tests/serve/test_config.py::test_channel_secrets_absent_from_openapi -v` 通過。
- [x] 1.3 [P] 為 Requirement「Webhook channel constructors fail fast on missing or blank secrets」呼應 Decision「D5 Channel secrets live in Settings as SecretStr fields」在 `cantus/serve/channels/_signing.py` 新增 helper `resolve_secret(constructor_arg: str | None, settings_field: SecretStr | None, field_name: str, provider: str) -> str`：constructor 參數優先、然後 settings；空字串或 whitespace 視同未設；缺失時 raise `ValueError` 含字面 `channel secret not configured` 與 `field_name` 字串。驗證：`pytest tests/serve/channels/test_signing.py::test_resolve_secret -v` 涵蓋四 case（兩來源都有 / 只 settings / 只 ctor / 兩者皆空）。

## 2. WebhookChannel Protocol 與 ChannelSendError

- [x] 2.1 為 Requirement「WebhookChannel Protocol extends Channel with mount(app)」呼應 Decision「D1 Sibling WebhookChannel Protocol extends Channel with mount」在 `cantus/serve/channel.py` 末尾新增 `WebhookChannel(Channel, Protocol)` 並加 `@runtime_checkable`，宣告 `def mount(self, app: FastAPI) -> None: ...`，FastAPI import 用 `TYPE_CHECKING` 守護避免 cantus.serve.channel 強拉 fastapi。驗證：`python -c "from cantus.serve.channel import Channel, WebhookChannel; class C: pass; print(isinstance(C(), WebhookChannel))"` 印 `False`；`mypy --strict cantus/serve/channel.py` 全綠。
- [x] 2.2 為 Requirement「WebhookChannel Protocol extends Channel with mount(app)」補測試證明 `LocalMockReceiver` 是 `Channel` 但不是 `WebhookChannel`，以及空 stub class 同時實作 `receive` + `send` + `mount` 三方法時兩 Protocol 都通過 `isinstance`。驗證：`pytest tests/serve/test_channel.py::test_webhook_channel_membership -v` 通過。
- [x] 2.3 在 `cantus/serve/channels/_errors.py` 新增 `ChannelSendError(Exception)` 類別，attributes `status_code: int`、`body_excerpt: str`、`provider: str`，`__init__` 接這三參數並組 `__str__` 為 `f"{provider} send failed: HTTP {status_code} {body_excerpt}"`，**禁止**在 `__str__` 引用任何 settings 物件或 secret 值。驗證：`pytest tests/serve/channels/test_errors.py::test_channelsenderror_no_secret_leak -v` 通過，含 `str(err)` 不含 placeholder fake token 字串。

## 3. 共用簽章工具 _signing.py

- [x] 3.1 為 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」呼應 Decision「D4 Signature verification failure returns indistinguishable 401 body」在 `cantus/serve/channels/_signing.py` 新增 `compute_line_signature(channel_secret: str, raw_body: bytes) -> str` ──回傳 `base64.b64encode(hmac.new(secret_bytes, raw_body, hashlib.sha256).digest()).decode()`。驗證：`pytest tests/serve/channels/test_signing.py::test_compute_line_signature_known_vector -v` 用 LINE 官方文件範例 (`channel_secret = "test_secret"`, body = 已知 raw bytes) 比對 digest 字串。
- [x] 3.2 為 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」與 Requirement「Telegram webhook verifies X-Telegram-Bot-Api-Secret-Token via constant-time compare」在 `_signing.py` 新增 `constant_time_compare(provided: str | None, expected: str) -> bool`──`provided is None` 回 `False`；UTF-8 encode 後走 `hmac.compare_digest`；wraps `UnicodeEncodeError`/`ValueError` 回 `False`（沿用 v0.4.1 `_check_token` pattern）。驗證：`pytest tests/serve/channels/test_signing.py::test_constant_time_compare -v` 四 case：相等回 True、不等回 False、`None` 回 False、surrogate-laden 字串回 False（不 raise）。
- [x] 3.3 為 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」在 `_signing.py` 新增高階 helper `verify_line_signature(channel_secret: str, raw_body: bytes, provided_signature: str | None) -> bool` 組合 1.3 + 3.1 + 3.2。驗證：`pytest tests/serve/channels/test_signing.py::test_verify_line_signature -v` 三 case（correct / wrong / missing）。

## 4. LineWebhookChannel 實作

- [x] 4.1 [P] 為 Requirement「Webhook channel constructors fail fast on missing or blank secrets」在 `cantus/serve/channels/line.py` 實作 `LineWebhookChannel.__init__(self, channel_secret=None, channel_access_token=None, queue_maxlen=None, settings=None)`──用 `_signing.resolve_secret` 解析兩個 secret 來源、初始化 `self._queue: deque[dict[str, Any]] = deque(maxlen=queue_maxlen)`、store secrets 於 `self._channel_secret: str` 與 `self._channel_access_token: str`、保留 `self._app: FastAPI | None = None`。驗證：`pytest tests/serve/channels/test_line.py::test_constructor_fail_fast -v` 三 case：兩 secret 都缺 / 只缺其一 / 都從 env 拿到。
- [x] 4.2 [P] 為 Requirement「WebhookChannel Protocol extends Channel with mount(app)」與 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」在 `LineWebhookChannel.mount(self, app)` 註冊 `POST /channels/line` async route handler、把 `app` reference 存到 `self._app`。route handler 讀 raw body bytes 一次、呼叫 `verify_line_signature`、失敗時 `raise HTTPException(401, detail="Authentication required")`、成功時 `json.loads(raw)` 後 push 進 `self._queue`、回 `{"ok": True}`。驗證：`pytest tests/serve/channels/test_line.py::test_mount_registers_route -v` 確認 route 存在於 `app.routes`、path 為 `/channels/line`、method 為 POST。
- [x] 4.3 [P] 為 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」用 `TestClient` 模擬完整 inbound flow：correct signature → 200、wrong signature → 401 + indistinguishable body、missing header → 401 + indistinguishable body、queue 內容正確。驗證：`pytest tests/serve/channels/test_line.py::test_inbound_signature_matrix -v` 三 case 全綠，且 401 response body 完全等於 `{"detail": "Authentication required"}`。
- [x] 4.4 [P] 為 Requirement「Channel queue exposes inbound events as raw platform dicts」實作 `LineWebhookChannel.receive(self) -> dict[str, Any]`──`popleft()` 內部 queue，空時 raise `IndexError("LineWebhookChannel queue is empty")`。驗證：`pytest tests/serve/channels/test_line.py::test_receive_pass_through -v` 確認 receive() 回 dict 與 raw JSON 完全相同（含 nested structure 不被改）；空 queue 拋 IndexError 訊息含 `queue is empty`。
- [x] 4.5 [P] 為 Requirement「Channel queue exposes inbound events as raw platform dicts」實作 `queue_maxlen` 行為：當 deque(maxlen=N) 達上限再 push 時，oldest 被丟掉，並透過 `logging.getLogger("cantus.serve.channels").warning(...)` 寫 warning。驗證：`pytest tests/serve/channels/test_line.py::test_queue_maxlen_drop_oldest -v` 用 `caplog` 抓到 WARNING 級別 log 訊息含 `dropped` 字串，且 `receive()` 拿到的是「較新」事件。
- [x] 4.6 [P] 為 Requirement「LineWebhookChannel.send POSTs to LINE Messaging API with Bearer auth」呼應 Decision「D6 Outbound httpx AsyncClient is app-scoped via FastAPI lifespan」實作 `async def send(self, message: dict[str, Any]) -> None`：從 `self._app.state.http_client` 拿 `httpx.AsyncClient`、POST 到 `https://api.line.me/v2/bot/message/reply`，header `Authorization: Bearer <access_token>` 與 `Content-Type: application/json`；2xx 回 None；4xx/5xx raise `ChannelSendError(status_code=resp.status_code, body_excerpt=resp.text[:200], provider="line")`。驗證：`pytest tests/serve/channels/test_line.py::test_send_outbound -v` 用 `respx` mock httpx，含 200 path 與 400 path。
- [x] 4.7 [P] 為 Requirement「LineWebhookChannel.send POSTs to LINE Messaging API with Bearer auth」補測試證明 `ChannelSendError.__str__` 不含 `channel_access_token` 字面值。驗證：`pytest tests/serve/channels/test_line.py::test_send_error_no_token_leak -v` 用 obviously-fake token `"line-token-XYZ"`，斷言 `"line-token-XYZ" not in str(err)`。

## 5. TelegramWebhookChannel 實作

- [x] 5.1 [P] 為 Requirement「Webhook channel constructors fail fast on missing or blank secrets」在 `cantus/serve/channels/telegram.py` 實作 `TelegramWebhookChannel.__init__(self, secret_token=None, bot_token=None, queue_maxlen=None, settings=None)`──結構與 LINE 對齊但用 `channel_telegram_secret_token` / `channel_telegram_bot_token` 兩個 settings 欄位。驗證：`pytest tests/serve/channels/test_telegram.py::test_constructor_fail_fast -v` 三 case 全綠。
- [x] 5.2 [P] 為 Requirement「WebhookChannel Protocol extends Channel with mount(app)」與 Requirement「Telegram webhook verifies X-Telegram-Bot-Api-Secret-Token via constant-time compare」實作 `TelegramWebhookChannel.mount(app)` 註冊 `POST /channels/telegram`；route handler 用 `_signing.constant_time_compare(request.headers.get("x-telegram-bot-api-secret-token"), self._secret_token)` 驗證、失敗 401、成功 push dict、回 `{"ok": True}`。驗證：`pytest tests/serve/channels/test_telegram.py::test_mount_registers_route -v` 確認 route path/method 正確。
- [x] 5.3 [P] 為 Requirement「Telegram webhook verifies X-Telegram-Bot-Api-Secret-Token via constant-time compare」inbound 矩陣測試：correct token / wrong token / missing header 三 case。驗證：`pytest tests/serve/channels/test_telegram.py::test_inbound_secret_matrix -v` 三 case 全綠，所有 401 body 等於 `{"detail": "Authentication required"}`。
- [x] 5.4 [P] 為 Requirement「Channel queue exposes inbound events as raw platform dicts」實作 `TelegramWebhookChannel.receive()` 與 queue_maxlen 行為。驗證：`pytest tests/serve/channels/test_telegram.py::test_receive_pass_through_and_maxlen -v` 通過。
- [x] 5.5 [P] 為 Requirement「TelegramWebhookChannel.send POSTs to Telegram Bot API」呼應 Decision「D6 Outbound httpx AsyncClient is app-scoped via FastAPI lifespan」實作 `async def send(self, message)`：POST 到 `f"https://api.telegram.org/bot{self._bot_token}/sendMessage"`，header `Content-Type: application/json`；4xx/5xx raise `ChannelSendError(provider="telegram", ...)`。驗證：`pytest tests/serve/channels/test_telegram.py::test_send_outbound -v` `respx` mock 200 與 403 path 全綠。
- [x] 5.6 [P] 為 Requirement「TelegramWebhookChannel.send POSTs to Telegram Bot API」測試 `str(err)` 不含 `bot_token`。驗證：`pytest tests/serve/channels/test_telegram.py::test_send_error_no_token_leak -v` 用 fake token `"123:tg-bot-XYZ"`，斷言該字串不出現在 exception message。

## 6. serve() 整合 webhook mount loop 與 httpx lifespan

- [x] 6.1 為 Requirement「/channels path prefix is reserved at app build time」呼應 Decision「D3 Webhook route prefix /channels/{provider} is reserved at build time」在 `cantus/serve/app.py` 把 `RESERVED_DASHBOARD_NAMES` 重構成 `RESERVED_TOP_LEVEL_NAMES = frozenset({"skills", "health", "events", "channels"})`，並調整 collision 檢查同時抓 `"channels"`；衝突 raise `ValueError` 含字面 `reserved channel path`（dashboard 衝突仍用既有字面 `reserved dashboard path`，雙錯誤訊息明確）。驗證：`pytest tests/serve/test_app.py::test_skill_name_channels_rejected -v` 通過、既有 dashboard 衝突 case 仍綠（regression）。
- [x] 6.2 為 Requirement「serve() mounts every WebhookChannel via Protocol-based dispatch」呼應 Decision「D1 Sibling WebhookChannel Protocol extends Channel with mount」在 `serve()` 註冊 dashboard route 之後加 webhook mount loop：`for c in channels: if isinstance(c, WebhookChannel): c.mount(app)`。`WebhookChannel` 從 `cantus.serve.channel` import（順序在檔案最上方）。驗證：`pytest tests/serve/test_app_webhook_integration.py::test_mount_loop_only_calls_webhook_channels -v` 用 `LocalMockReceiver` + spy `WebhookChannel` 雙實例驗證只 spy 被呼叫一次。
- [x] 6.3 為 Requirement「Cantus serve ships webhook channel gateways for LINE and Telegram」呼應 Decision「D6 Outbound httpx AsyncClient is app-scoped via FastAPI lifespan」用 FastAPI `lifespan` async context manager 建立 `app.state.http_client = httpx.AsyncClient(timeout=10.0)`，shutdown 時 `await app.state.http_client.aclose()`。`serve()` 把 lifespan 傳入 `FastAPI(lifespan=...)`。驗證：`pytest tests/serve/test_app_webhook_integration.py::test_httpx_lifespan -v` 用 `with TestClient(app) as client` 進入 lifespan、驗證 `app.state.http_client` 是 `httpx.AsyncClient` 實例，離開 context 後 `app.state.http_client.is_closed` 為 True。
- [x] 6.4 為 Requirement「Cantus serve ships webhook channel gateways for LINE and Telegram」整合測試：同一個 `serve()` 同時帶 `LocalMockReceiver` + `LineWebhookChannel` + 一個 Skill，並驗證所有 endpoint 路徑各自獨立（`POST /skills/<name>` / `GET /health` / `POST /channels/line`）。驗證：`pytest tests/serve/test_app_webhook_integration.py::test_coexistence_with_existing_routes -v` 全綠。

## 7. cantus.serve public API 與 __all__ 更新

- [x] 7.1 為 Requirement「cantus[serve] extras include httpx and webhook channels are importable」修改 `cantus/serve/__init__.py` 加 4 個 re-export：`WebhookChannel`（從 `cantus.serve.channel`）、`LineWebhookChannel`（從 `cantus.serve.channels.line`）、`TelegramWebhookChannel`（從 `cantus.serve.channels.telegram`）、`ChannelSendError`（從 `cantus.serve.channels._errors`）；更新 `__all__`。驗證：`python -c "from cantus.serve import WebhookChannel, LineWebhookChannel, TelegramWebhookChannel, ChannelSendError"` 不噴 error；`pytest tests/serve/test_init_exports.py::test_webhook_symbols_exported -v` 通過。
- [x] 7.2 為 Requirement「cantus[serve] extras include httpx and webhook channels are importable」修改 `pyproject.toml` 的 `[project.optional-dependencies] serve` 列表加 `httpx>=0.27,<1`；不動 `[tool.uv].conflicts`。驗證：`grep -E "^[ ]*\"httpx>=0.27,<1\"" pyproject.toml` 命中、`uv pip install -e .[serve] --dry-run` 列出 httpx；MIGRATION 文件中聲明 ADDITIVE。

## 8. 完整整合與回歸測試

- [x] 8.1 [P] 為 Requirement「serve() mounts every WebhookChannel via Protocol-based dispatch」與 Requirement「/channels path prefix is reserved at app build time」補回歸：v0.4.4 既有所有 `tests/serve/test_app.py` / `tests/serve/test_security.py` / `tests/serve/test_dashboard.py` 全部 case 都應綠（ADDITIVE）。驗證：`pytest tests/serve/ -v --ignore=tests/serve/channels` 全綠，無新 failure。
- [x] 8.2 [P] 為 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」與 Requirement「Telegram webhook verifies X-Telegram-Bot-Api-Secret-Token via constant-time compare」呼應 Decision「D4 Signature verification failure returns indistinguishable 401 body」加 cross-platform 401 indistinguishable 確認：LINE 三種失敗（missing header / wrong digest / empty header value）與 Telegram 兩種失敗（missing / wrong），五個 response body 全部 byte-identical 等於 v0.4.1 `require_auth` 失敗 body。驗證：`pytest tests/serve/channels/test_401_indistinguishability.py -v` 五 case 全綠。
- [x] 8.3 [P] 為 Requirement「Cantus serve ships webhook channel gateways for LINE and Telegram」呼應 Decision「D7 Inbound event payload pass-through as dict」加 payload pass-through 確認：webhook handler 不對 raw JSON 做任何 normalization；nested dict / array / unicode 全部原樣 push。驗證：`pytest tests/serve/channels/test_payload_pass_through.py -v` 用 LINE 文件範例 payload 與 Telegram 文件範例 payload 各跑一次，比對 `receive()` 回值 deep-equal 輸入。
- [x] 8.4 [P] 為 Requirement「WebhookChannel Protocol extends Channel with mount(app)」呼應 Decision「D2 Platform scope limited to LINE and Telegram」加 import-time 確認：`from cantus.serve.channels import line, telegram` 都成功，但 `cantus.serve.channels` 命名空間下不存在 `googlechat` 或 `discord` 子模組。驗證：`pytest tests/serve/channels/test_module_surface.py -v` 三 assertions（line 可 import / telegram 可 import / googlechat & discord ImportError）。
- [x] 8.5 [P] 為 Requirement「Cantus serve ships webhook channel gateways for LINE and Telegram」`mypy --strict cantus/serve/` 全綠──含新增 `channels/` 子套件。驗證：`mypy --strict cantus/serve/` 退出碼 0、無新 warning。

## 9. 文件、CHANGELOG 與 MIGRATION

- [x] 9.1 為 Requirement「LineWebhookChannel.send POSTs to LINE Messaging API with Bearer auth」與 Requirement「LINE webhook verifies X-Line-Signature via HMAC-SHA256 over raw body」新增 `docs/cookbook-line-channel.md`：學生視角 echo bot walkthrough，含 env 變數設定（兩個 secret）、`LineWebhookChannel()` 建立、`cantus serve --channels myapp:line_ch` 啟動、Cloudflare Tunnel 暴露（cross-ref A1' walkthrough）、LINE Developers Console 後台 webhook URL 註冊步驟、curl 自我測試命令。驗證：`/humane-prose-audit docs/cookbook-line-channel.md` 不出現任何 placeholder（無 TODO/TBD/「…」）、學生 path 從頭到尾可順讀。
- [x] 9.2 為 Requirement「TelegramWebhookChannel.send POSTs to Telegram Bot API」與 Requirement「Telegram webhook verifies X-Telegram-Bot-Api-Secret-Token via constant-time compare」新增 `docs/cookbook-telegram-channel.md`：同 LINE 結構，但平台後台改為 `@BotFather` 取 bot_token 與 `setWebhook` 命令範例。驗證：`/humane-prose-audit docs/cookbook-telegram-channel.md` 通過、curl `setWebhook` 命令可直接複製貼上。
- [x] 9.3 為 Requirement「Cantus serve ships webhook channel gateways for LINE and Telegram」呼應 Decision「D2 Platform scope limited to LINE and Telegram」新增 `MIGRATION_v0.4.4_to_v0.4.5.md`：列出四個新 env 變數、`[serve]` extras 加 httpx、新 public symbols、聲明 ADDITIVE 不破壞 v0.4.4，並明寫「Google Chat 走 B3 Pub/Sub、Discord 走 B2 WebSocket」避免使用者誤期待。驗證：人工 review 涵蓋四個 env 變數列表、httpx 版本 pin、四 public symbols 完整。
- [x] 9.4 為 Requirement「cantus[serve] extras include httpx and webhook channels are importable」更新 `CHANGELOG.md`：加 `## [0.4.5] - 2026-05-xx` 段，含 Added（四 public symbols / 四 env 欄位 / `/channels/{provider}` reserved path / httpx dep）、Changed（`RESERVED_DASHBOARD_NAMES` 重構為 `RESERVED_TOP_LEVEL_NAMES`，外觀差異列為 internal，仍 ADDITIVE 對外）、Notes（B 系列下一站 B2 Discord）。驗證：CHANGELOG 條目符合 `tw-emoji-release-note` 風格、版號順序正確、含 release date placeholder。
- [x] 9.5 為 Requirement「Cantus serve ships webhook channel gateways for LINE and Telegram」呼應 Decision「D2 Platform scope limited to LINE and Telegram」最終 `spectra validate cantus-channel-gateway-webhook` 全綠、`spectra analyze cantus-channel-gateway-webhook --json` 無 Critical / Warning 級 finding。驗證：兩條命令各自回 exit code 0、輸出無 `"severity": "critical"` 或 `"severity": "warning"` JSON 欄位。
