<!--
顆粒度與順序原則：
- TDD：每個 finding 的 implementation section 都「先寫 test → 觀察 RED → 寫 code 過綠 → ruff/mypy clean」。
- [P] 標記用於明顯可平行的工作：不同檔案、無共享 state、不互相依賴。
- 每個 task 行尾的「驗證」段落是 acceptance signal，apply agent 必須能直接執行。
-->

## 1. Prerequisites

- [x] 1.1 [P] 確認 worktree 與 branch 狀態：`pwd` 為 `.worktrees/cantus-gate-b-audit`、`git branch --show-current` 為 `cantus-gate-b-audit`、`git status` clean、`openspec/changes/gate-b-audit-hardening/` 與其下 `proposal.md` / `design.md` / `specs/cantus-channel-gateway-{webhook,realtime,pubsub}/spec.md` 皆存在。驗證：上述 4 條命令逐一執行，全部 exit 0 且輸出符合預期；`ls openspec/changes/gate-b-audit-hardening/specs/cantus-channel-gateway-{webhook,realtime,pubsub}/spec.md` 三條皆 exist。
- [x] 1.2 [P] 讀完 `audit/gate-b/SUMMARY.md`、`audit/gate-b/spectra-pubsub.md`（M4 spec drift 詳細分析）、`design.md` 全部 9 條 Decisions，並對 *Spec drift 修正方向：改 code+test，不改 source spec* 與 *MODIFIED Requirement 顆粒度 = 1 per Requirement title（共 3，非 5）* 兩條決策有明確心智模型。驗證：能於後續 task 描述中引用這兩條決策；本 task 完成的證據是後續 sections 2-8 任務開始執行。

## 2. M1 — TelegramWebhookChannel 強化「Webhook channel constructors fail fast on missing or blank secrets」（採 *M1 Telegram token format validation 採嚴格 regex + length* 決策）

- [x] 2.1 在 `tests/serve/channels/test_telegram.py` 新增 `test_bot_token_format_*` 與 `test_secret_token_format_*` 一組測試：覆蓋 (a) 合法 bot_token + secret_token 構造成功、(b) bot_token 缺 `<digits>:` 前綴 raise `ValueError` 且訊息含 `telegram bot_token has invalid format` 並 NOT 含輸入值、(c) bot_token suffix 短於 20 字元 raise、(d) bot_token 長度 > 255 raise、(e) secret_token 含空白 / `!@#` raise 且訊息含 `telegram secret_token has invalid format` 並 NOT 含輸入值、(f) secret_token 長度 > 256 raise、(g) 既有「missing → channel secret not configured」訊息不被新檢查取代。驗證：`pytest tests/serve/channels/test_telegram.py -k "format" -x` 先觀察 RED，code 未改動前所有新 test 都 fail。
- [x] 2.2 在 `cantus/serve/channels/telegram.py` 的 `TelegramWebhookChannel.__init__` 內，於 `resolve_secret()` 解析完兩個 token 後新增 format validation：bot_token 套 `re.fullmatch(r"^\d+:[A-Za-z0-9_-]{20,}$", value) and len(value) <= 255`；secret_token 套 `re.fullmatch(r"^[A-Za-z0-9_-]+$", value) and 1 <= len(value) <= 256`。違反任一條件 raise `ValueError`，訊息字面為「telegram bot_token has invalid format」或「telegram secret_token has invalid format」（**不**插入輸入值）。驗證：`pytest tests/serve/channels/test_telegram.py -x` 全綠；`ruff check cantus tests` 與 `mypy cantus tests` 對 telegram 模組 clean。
- [x] 2.3 更新所有 `tests/serve/channels/conftest.py` 或既有 fixture 中使用 toy bot_token / secret_token 的位置，改用合法占位（例如 bot_token `"123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ_-"`、secret_token `"Sample_Secret-Token"`），確保既有測試在新 validation 後仍可通過。本 section 2 的三個 task 共同實現 spec Requirement「Webhook channel constructors fail fast on missing or blank secrets」新增的 Telegram format-validation 子句。驗證：`pytest tests/serve` 全綠，無新 fail。

## 3. M2 — Discord HELLO heartbeat 範圍檢查（鎖入 *Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff* Requirement；採 *M2 Discord HELLO heartbeat 範圍 100ms..120s* 決策）

- [x] 3.1 在 `tests/serve/channels/test_realtime.py` 新增 `test_hello_heartbeat_bounds_*` 一組測試：覆蓋 (a) heartbeat_interval=0 → 觀察到 `_ResumableError` 被 raise 且 HEARTBEAT 迴圈未啟動、(b) heartbeat_interval=99 → 同上、(c) heartbeat_interval=100 → 接受、(d) heartbeat_interval=41250 → 接受（典型 Discord 值）、(e) heartbeat_interval=120000 → 接受、(f) heartbeat_interval=120001 → raise、(g) heartbeat_interval=200000 → raise。驗證：`pytest tests/serve/channels/test_realtime.py -k "heartbeat_bounds" -x` 先 RED。
- [x] 3.2 在 `cantus/serve/channels/_realtime.py` 處理 HELLO frame 的位置（既有從 `hello["d"]["heartbeat_interval"]` 抽值的地方），在抽值之後、`self._heartbeat_interval_ms` 賦值之前加入 `if not (100 <= interval_ms <= 120000): raise _ResumableError(...)`。此 task 與 section 4 的 helper 抽取共同實現 spec Requirement「Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff」新增的 heartbeat bounds 子句與 DISPATCH-only seq 子句。驗證：`pytest tests/serve/channels/test_realtime.py -x` 全綠；`ruff check` 與 `mypy` 對 `_realtime.py` clean。

## 4. M3 — Discord seq 推進透過 `_accept_dispatch_frame(frame)` helper（鎖入 *Discord Gateway WebSocket connect implements IDENTIFY, HEARTBEAT, RESUME, and exponential backoff* Requirement；採 *M3 抽 `_accept_dispatch_frame(frame)` helper（非 inline comment）* 決策）

- [x] 4.1 在 `tests/serve/channels/test_realtime.py` 新增 `test_seq_only_advances_on_dispatch_*` 一組測試：覆蓋 (a) `op = 11` (HEARTBEAT ACK) 帶 `{"s": 999}` payload → `self._seq` 不變、(b) `op = 0` (DISPATCH) 帶 `{"s": 43, "t": "MESSAGE_CREATE", "d": {...}}` → `self._seq` 變 43、(c) `_accept_dispatch_frame` 對 (a) 案例**不**被呼叫，對 (b) 案例恰被呼叫一次。驗證：`pytest tests/serve/channels/test_realtime.py -k "seq_only_advances" -x` 先 RED。
- [x] 4.2 在 `cantus/serve/channels/_realtime.py` 抽出 `def _accept_dispatch_frame(self, frame: Mapping[str, Any]) -> None:` private helper，內部承接 (i) `self._seq` 推進（`isinstance(s := frame.get("s"), int)` 才更新）、(ii) dispatcher routing（既有 `t` field 對 handler 的查表/呼叫邏輯，若有）。重構 frame 主迴圈：op==DISPATCH 分支由直接寫 seq 改為呼叫 `self._accept_dispatch_frame(frame)`；非 DISPATCH 分支絕對不呼叫此 helper、絕對不寫 `self._seq`。驗證：`pytest tests/serve/channels/test_realtime.py -x` 全綠；`grep -n "self\._seq" cantus/serve/channels/_realtime.py` 顯示 `self._seq =` 賦值只出現在 `_accept_dispatch_frame` 內。

## 5. M4 — Pub/Sub failure counter 對齊「connect() opens a Pub/Sub streaming pull, acks after enqueue, and applies exponential backoff with a ten-failure ceiling」Requirement（採 *M4 instance flag 命名 `_success_since_last_failure`*、*M4 race-condition 評估：flag set/read 不需 lock*、*Spec drift 修正方向：改 code+test，不改 source spec* 三條決策）

- [x] 5.1 在 `tests/serve/channels/test_googlechat_callback.py`（若該檔不存在，沿用既有 googlechat test 檔命名規則新增）新增 `test_success_resets_failure_counter` 一組測試覆蓋 spec scenario「Successful delivery resets the failure counter」與新增 scenario「The success-since-last-failure flag is set after ack and cleared in the except branch」：覆蓋 (a) 5 連續失敗 → 1 則訊息成功 ack → 第 6 次失敗時 `asyncio.sleep` duration 為 1 秒（而非 64 秒）、(b) flag 在 ack 後變 True、在 except 分支被 consumed 後變回 False、(c) 完全沒有成功訊息時 flag 維持 False 且 attempts 持續累進。驗證：`pytest tests/serve/channels/test_googlechat_callback.py -k "success_resets or flag" -x` 先 RED。
- [x] 5.2 在 `cantus/serve/channels/googlechat.py` `GoogleChatPubSubChannel.__init__` 新增 `self._success_since_last_failure: bool = False`；在 `_on_message` 的 `message.ack()` 呼叫之後（成功 enqueue 分支末）設 `self._success_since_last_failure = True`；在 `connect()` 主迴圈的 except 分支內，於 `attempts += 1` 之前加入 `if self._success_since_last_failure: attempts = 0; self._success_since_last_failure = False`。`source spec` `openspec/specs/cantus-channel-gateway-pubsub/spec.md` 全程不動。本 section 5 與 section 6 共同實現 spec Requirement「connect() opens a Pub/Sub streaming pull, acks after enqueue, and applies exponential backoff with a ten-failure ceiling」新增的 success-flag 子句與 explicit pubsub scope 子句。驗證：`pytest tests/serve/channels/test_googlechat*.py -x` 全綠；`git diff openspec/specs/cantus-channel-gateway-pubsub/spec.md` 空 diff；`ruff check` 與 `mypy` 對 googlechat 模組 clean。

## 6. L1 — Pub/Sub `_build_subscriber()` 顯式宣告 OAuth scope（同上 Requirement；採 *L1 不額外加 send()-path scopes 比對 test* 決策）

- [x] 6.1 在 `tests/serve/channels/test_googlechat_callback.py` 新增 `test_build_subscriber_passes_explicit_pubsub_scope` 測試：以 mock 攔截 `google.oauth2.service_account.Credentials.from_service_account_file`，assert 呼叫時 keyword 含 `scopes=["https://www.googleapis.com/auth/pubsub"]`；同時 assert 回傳 SubscriberClient credentials.scopes 含該字串。**不**新增 send()-path scope 比對 test（既有 spec 已覆蓋）。驗證：`pytest tests/serve/channels/test_googlechat_callback.py -k "explicit_pubsub_scope" -x` 先 RED。
- [x] 6.2 在 `cantus/serve/channels/googlechat.py` `_build_subscriber()` 的 `_Creds.from_service_account_file(self._credentials_path)` 呼叫改為 `_Creds.from_service_account_file(self._credentials_path, scopes=["https://www.googleapis.com/auth/pubsub"])`。驗證：`pytest tests/serve/channels/test_googlechat*.py -x` 全綠；`ruff check` 與 `mypy` 對 googlechat 模組 clean。

## 7. 版本欄位 — DEFERRED（採 *tasks.md Section 7（version bump）標 deferred* 決策）

- [ ] 7.1 **不執行**：本 change 不修 `pyproject.toml` 的任何版本欄位、不修 `CHANGELOG.md` 的三條 0.4.5/0.4.6/0.4.7 草稿、不修 `MIGRATION.md`。版本 bump 至 v0.4.7 由 release plan 一次性處理。驗證：apply 結束時執行 `git diff --name-only pyproject.toml CHANGELOG.md MIGRATION.md` 必須無輸出；該 task 永遠保持 unchecked，archive 階段對此 task 的存在性給予 audit trail。

## 8. Integration validation

- [x] 8.1 全套 channel tests 綠燈，驗證 design.md *Implementation Contract → Behaviour（end user / operator 視角）* 段落列出的所有觀察結果（telegram constructor reject、heartbeat bounds reject、seq 不被 non-DISPATCH 推進、6th-failure-after-ack sleep≈1s、SubscriberClient 帶 explicit scope）與 *Failure modes* 段列出的所有 raise type（`ValueError`、`_ResumableError`）皆被測試直接 assert：執行 `pytest tests/serve/channels -x --tb=short` 通過，輸出末行顯示 `passed` 且無 `failed` / `errored`。驗證：紀錄 pytest 終端輸出末 5 行於 apply 階段 commit message 或 PR 描述。
- [x] 8.2 Lint 與 type clean，驗證 design.md *Implementation Contract → Interfaces / data shape* 段宣告的所有 method signatures（`_accept_dispatch_frame(self, frame: Mapping[str, Any]) -> None`、`_success_since_last_failure: bool`、`_build_subscriber` 新 keyword）通過 mypy 型別檢查：執行 `ruff check cantus tests` exit 0、`mypy cantus tests` exit 0、無新引入的 `# type: ignore` 或 `# noqa` 註解（`git diff` 對這兩個 token 不出現新增行）。驗證：兩條命令 exit 0；`git diff -U0 | grep -E "^\+.*(# type: ignore|# noqa)"` 為空。
- [x] 8.3 Source spec 零 drift，驗證 design.md *Implementation Contract → Scope boundaries* 段落列出的所有 out-of-scope 檔案（含三份 source spec）皆未被改動：執行 `git diff openspec/specs/cantus-channel-gateway-webhook/spec.md`、`git diff openspec/specs/cantus-channel-gateway-realtime/spec.md`、`git diff openspec/specs/cantus-channel-gateway-pubsub/spec.md` 三條皆空 diff，同時 `git diff -- pyproject.toml CHANGELOG.md MIGRATION.md` 亦空 diff。驗證：上述命令輸出長度為 0。
- [x] 8.4 Spectra validate / analyze 全綠，作為 design.md *Implementation Contract → Acceptance criteria（apply / reviewer 視角）* 段列出的「formal acceptance check」：執行 `spectra validate gate-b-audit-hardening` 與 `spectra analyze gate-b-audit-hardening --json | python -c "import json,sys; r=json.load(sys.stdin); assert not any(f['severity'] in ('Critical','Warning') for f in r.get('findings', [])), r"`，皆 exit 0。驗證：兩條命令 exit 0；analyze 輸出無 Critical / Warning。
