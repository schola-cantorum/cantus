## 1. 紅燈測試（先寫測試、後實作）

- [x] 1.1 [P] 在 `libs/cantus/tests/test_distribution_config.py`（新檔）新增 `test_py_typed_marker_exists`：用 `importlib.resources.files('cantus').joinpath('py.typed')` 取 marker handle，assert `is_file()` 為 True、`read_text() == ''`。此測試覆蓋 `Cantus ships PEP 561 py.typed marker and baseline tool configuration` Requirement 的「py.typed is shipped in the wheel」scenario，並驗證 design 的 Decision 5: Spec 變更走 cantus-distribution Modified Capability（將此 Requirement 加入 cantus-distribution 而非新建 quality-baseline capability）的 spec 路由決策。驗證：`pytest libs/cantus/tests/test_distribution_config.py::test_py_typed_marker_exists -v` 在 py.typed 尚未建立時應**紅燈**（FileNotFoundError or assert fail）。
- [x] 1.2 [P] 在同檔新增 `test_pyproject_has_setuptools_package_data_for_py_typed`：用 `tomllib.load` 解析 `libs/cantus/pyproject.toml`，assert `tool.setuptools.package-data.cantus` 為 list 且包含 `"py.typed"`。覆蓋同 Requirement 的「py.typed is shipped in the wheel」scenario 的 wheel-build 子契約。驗證：`pytest .../test_pyproject_has_setuptools_package_data_for_py_typed -v` 在 pyproject 尚未補 package-data 段時應紅燈（KeyError or assert fail）。
- [x] 1.3 [P] 在同檔新增 `test_pyproject_has_mypy_baseline`：assert `tool.mypy.python_version == "3.10"`、`warn_unused_ignores is True`、`warn_redundant_casts is True`、`check_untyped_defs is True`、`disallow_untyped_defs is False`，並 assert 至少包含一個 `[[tool.mypy.overrides]]` entry 其 `module` 字串 list 含 `"mcp.*"` 與 `ignore_missing_imports is True`。覆蓋同 Requirement 的「pyproject ships mypy baseline」scenario。驗證：對應 pytest 應紅燈。
- [x] 1.4 [P] 在同檔新增 `test_pyproject_has_coverage_baseline`：assert `tool.coverage.run.source == ["cantus"]`、`tool.coverage.run.branch is True`、`tool.coverage.report.show_missing is True`、`tool.coverage.report.exclude_lines` list 包含 `"pragma: no cover"` 與 `"if TYPE_CHECKING:"`。覆蓋同 Requirement 的 coverage scenario。驗證：對應 pytest 應紅燈。
- [x] 1.5 [P] 在同檔新增 `test_pytest_addopts_triggers_cov`：assert `tool.pytest.ini_options.addopts` 為字串且同時包含 substring `"--cov=cantus"` 與 `"--cov-report=term-missing"`。覆蓋「pytest emits coverage output by default」scenario 的契約核心。驗證：對應 pytest 應紅燈（既有 addopts 為 `"-v --tb=short"`）。
- [x] 1.6 [P] 在同檔新增 `test_pyproject_version_bumped_to_0_3_5`：assert `project.version == "0.3.5"`，作為 ADDITIVE release 的版本錨點。驗證：對應 pytest 應紅燈（pyproject 當前為 `"0.3.4"`）。

## 2. py.typed marker 實作

- [x] 2.1 建立 `libs/cantus/cantus/py.typed` 為 0 byte 空檔（PEP 561 inline-typed package convention），落實 design Decision 1: `py.typed` 採用空檔 marker（PEP 561 標準作法），讓 `Cantus ships PEP 561 py.typed marker and baseline tool configuration` Requirement 的 marker 契約成立。驗證：`stat -f '%z' libs/cantus/cantus/py.typed`（或 `wc -c < libs/cantus/cantus/py.typed`）回傳 `0`；`pytest libs/cantus/tests/test_distribution_config.py::test_py_typed_marker_exists -v` 變綠。

## 3. pyproject.toml 設定

- [x] 3.1 [P] 在 `libs/cantus/pyproject.toml` 新增 `[tool.setuptools.package-data]` 段並設定 `cantus = ["py.typed"]`，讓 wheel build 帶上 marker（落實 design Decision 1 的 setuptools package-data 路徑而非 include-package-data）。驗證：`pytest libs/cantus/tests/test_distribution_config.py::test_pyproject_has_setuptools_package_data_for_py_typed -v` 變綠。
- [x] 3.2 [P] 在同檔新增 `[tool.mypy]` 段並設定 `python_version = "3.10"`、`warn_unused_ignores = true`、`warn_redundant_casts = true`、`check_untyped_defs = true`、`disallow_untyped_defs = false`、`ignore_missing_imports = false`；再為 optional adapter SDK 透過 `[[tool.mypy.overrides]]` 設 `module = ["mcp.*", "langchain_core.*", "dspy.*", "transformers.*", "openhands.*", "anthropic.*", "openai.*", "google.genai.*", "groq.*"]` 與 `ignore_missing_imports = true`。落實 design Decision 2: mypy 採 `strict = false` 起步，僅啟用 4 個 warning flag 的「baseline 不啟用 strict、optional adapter 明列 override」決策，覆蓋 `Cantus ships PEP 561 py.typed marker and baseline tool configuration` Requirement 的「pyproject ships mypy baseline」scenario。驗證：`pytest libs/cantus/tests/test_distribution_config.py::test_pyproject_has_mypy_baseline -v` 變綠。
- [x] 3.3 [P] 在同檔新增 `[tool.coverage.run]` 段（`source = ["cantus"]`、`branch = true`、`omit = ["tests/*", "*/__pycache__/*"]`）與 `[tool.coverage.report]` 段（`show_missing = true`、`skip_covered = false`、`exclude_lines = ["pragma: no cover", "if TYPE_CHECKING:", "raise NotImplementedError"]`）。落實 design Decision 3: coverage 設定不設 fail-under，預設輸出 term-missing + xml 雙報告 的「branch 覆蓋啟用、不設 fail-under」決策。驗證：`pytest libs/cantus/tests/test_distribution_config.py::test_pyproject_has_coverage_baseline -v` 變綠。
- [x] 3.4 在同檔修改 `[tool.pytest.ini_options].addopts` 字串：將 `"-v --tb=short"` 改為 `"-v --tb=short --cov=cantus --cov-report=term-missing --cov-report=xml"`，讓 `pytest` 預設帶 coverage 輸出。覆蓋「pytest emits coverage output by default」scenario。驗證：`pytest libs/cantus/tests/test_distribution_config.py::test_pytest_addopts_triggers_cov -v` 變綠。
- [x] 3.5 在同檔將 `[project] version` 字串從 `"0.3.4"` bump 至 `"0.3.5"`，落實 ADDITIVE release 版本錨點。驗證：`pytest libs/cantus/tests/test_distribution_config.py::test_pyproject_version_bumped_to_0_3_5 -v` 變綠；`python -c "import tomllib; print(tomllib.load(open('libs/cantus/pyproject.toml','rb'))['project']['version'])"` 印 `0.3.5`。

## 4. adapters-batch2.md supersede note

- [x] 4.1 在 `libs/cantus/docs/protocols/adapters-batch2.md` 開頭（first H1 標題 `# adapters-batch2` 之後）插入一段以 `**Status:**` 開頭的 markdown 段，內容明示「Superseded by `adapters-batch3.md` (cantus v0.3.4) for the HuggingFace and OpenHands import directions; preserved as a v0.3.3 historical snapshot of the batch2 surface.」並 cross-link `[adapters-batch3.md](adapters-batch3.md)`。原 batch2 spec 本文 byte-identical 保留。落實 design Decision 4: `adapters-batch2.md` 加 supersede note 但保留全文 的「保留全文 + 加 supersede note」決策。驗證：`head -10 libs/cantus/docs/protocols/adapters-batch2.md | grep -F '**Status:**'` 至少回傳一筆；`grep -F 'adapters-batch3.md' libs/cantus/docs/protocols/adapters-batch2.md` 至少回傳一筆；`diff <(sed -n '/^## /,$p' libs/cantus/docs/protocols/adapters-batch2.md) <(git -C libs/cantus show HEAD:docs/protocols/adapters-batch2.md | sed -n '/^## /,$p')` 回傳零差異（本文 byte-identical）。

## 5. CHANGELOG 與 MIGRATION 文件

- [x] 5.1 在 `libs/cantus/CHANGELOG.md` 新增 v0.3.5 entry：標題為 `## [0.3.5] — 2026-05-18 — Quality Baseline`（或當天日期），內容列出四項 Added：「PEP 561 `py.typed` marker shipped under `cantus/py.typed`」、「baseline `[tool.mypy]` configuration」、「baseline `[tool.coverage.*]` configuration with branch coverage and term-missing report」、「supersede note on `docs/protocols/adapters-batch2.md` pointing to `adapters-batch3.md`」。明示「ADDITIVE — no BREAKING change, no new dependencies, no new optional extras, no cantus public callable change.」落實「Cantus ships PEP 561 py.typed marker and baseline tool configuration」Requirement 的 release 紀錄。驗證：`grep -F '[0.3.5]' libs/cantus/CHANGELOG.md` 至少回傳一筆；`grep -F 'py.typed' libs/cantus/CHANGELOG.md | head -1` 至少回傳一筆。
- [x] 5.2 [P] 建立 `libs/cantus/MIGRATION_v0.3.4_to_v0.3.5.md`：標題 `# Migrating cantus v0.3.4 → v0.3.5`，內容含 (a) ADDITIVE 聲明「No host code change required」；(b) `from cantus import ...`、`from cantus.hooks import ...`、`from cantus.workflows import ...`、`from cantus.adapters import ...` 既有 import 路徑全部 byte-identical 的明示；(c) dev workflow 改善 bullet（`pytest` 預設帶 cov、`mypy cantus` 有 baseline、wheel 帶 `py.typed`）；(d) 一行 `mypy cantus` 範例命令；(e) 提醒「新增 optional adapter SDK 時需同步 `[[tool.mypy.overrides]]` 列表」。驗證：`grep -F 'No host code change required' libs/cantus/MIGRATION_v0.3.4_to_v0.3.5.md` 至少回傳一筆。

## 6. 端到端驗證

- [x] 6.1 在 `libs/cantus/` 內執行 `pytest tests/test_distribution_config.py -v` 全綠（六個 1.x 紅燈測試全部轉綠）。
- [x] 6.2 在 `libs/cantus/` 內執行 `pytest tests/ -q` 一次（不指定 path），確認既有 testsuite 仍全綠且 stdout 末段含 coverage 區段（出現 `"---------- coverage"` 或 `"Name              Stmts   Miss"` 表頭）；同時確認工作目錄產生 `coverage.xml`。覆蓋「pytest emits coverage output by default」scenario 的整體行為契約。
- [x] 6.3 [P] 在 `libs/cantus/` 內執行 `mypy cantus`，確認 exit code 為 0 或 1（允許 warning，但不允許 mypy 配置解析失敗或內部 crash），stdout/stderr SHALL NOT 含 `"Cannot find implementation or library stub for module named \"mcp\""` 等被 override 涵蓋的 missing-import 錯誤。覆蓋「pyproject ships mypy baseline that lets `mypy cantus` run without import failures」scenario。
- [x] 6.4 [P] 端對端確認 wheel 帶 py.typed：在 `libs/cantus/` 內執行 `python -m build --wheel`（若 build 模組可用），用 `unzip -l dist/cantus-0.3.5*.whl | grep py.typed` 確認 wheel 內含 `cantus/py.typed`；若 `build` 模組不可用，改以 `python -c "from importlib.resources import files; print(repr(files('cantus').joinpath('py.typed').read_text()))"` 在 source layout 內驗證 marker 可達且為空字串。覆蓋「py.typed is shipped in the wheel」scenario 的 wheel-presence 子契約。
- [x] 6.5 全域檢查：`spectra validate cantus-quality-baseline` 通過、`spectra analyze cantus-quality-baseline --json` 無 Critical / Warning finding。
