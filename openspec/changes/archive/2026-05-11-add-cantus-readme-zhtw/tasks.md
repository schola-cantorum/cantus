## 1. 撰寫繁體中文 README

- [x] [P] 1.1 建立 `libs/cantus/README.zhTW.md`，章節順序與 `libs/cantus/README.md` 一一對應（hero banner → badge bar → Open-in-Colab CTA → Install → 30-second Quickstart → protocols banner → 五協定一句話介紹 → Documentation → License），文字以台灣慣用繁體中文撰寫，並在前 30 行內嵌入一條 visible text 含 `English`、href 為 repo-relative `README.md` 的語言切換連結。對應 `Cantus README ships a Traditional Chinese variant with bidirectional language switch` 的 Scenario「README.zhTW.md exists with the mandated section structure」與 bidirectional switch 的 zhTW → en 方向。驗證：對 `libs/cantus/README.zhTW.md` 連續執行 `head -n 30` 後可看到 `assets/banner_hero.jpeg`、含 `ECL-2.0` 字串的 badge、含 `colab.research.google.com/github/schola-cantorum/cantus/blob/` 的 hyperlink、以及 `[English](README.md)` 形式的回鏈；對整檔 grep 可命中 `assets/banner_protocols.jpeg`、五個協定（Skill/Analyzer/Validator/Workflow/Memory）的中文一句話介紹、含 `ECL-2.0` 字串的 License 章節；目視檢查未出現中國大陸用語（軟件／網絡／視頻／默認／用戶…）。

## 2. 在英文 README 接上「繁體中文」入口

- [x] [P] 2.1 在 `libs/cantus/README.md` 既有 hero/badge 區塊（檔案前 30 行內）插入一條 visible text 含 `繁體中文`、href 為 repo-relative `README.zhTW.md` 的語言切換連結，且 `README.md` 既有 hero banner 影像引用、所有 badge URL、Install pip 指令、30-second Quickstart Python 程式碼、五協定一句話介紹、Documentation 連結與 License 章節皆保持原樣未修改。對應 `Cantus README ships a Traditional Chinese variant with bidirectional language switch` 的 Scenario「README.md non-localized content is unchanged except for the language-switch link」。驗證：`head -n 30 libs/cantus/README.md` 顯示形如 `[繁體中文](README.zhTW.md)` 的連結；對 `libs/cantus/README.md` 跑 `git diff -- libs/cantus/README.md` 僅顯示與該語言切換連結相關的新增行、無任何 badge、banner、code block、protocol 介紹、License 行的內容變動。

## 3. 雙語對等性驗收

- [x] [P] 3.1 確認 Install 指令區塊（三個 pip 指令）、30-second Quickstart Python 區塊在 `libs/cantus/README.md` 與 `libs/cantus/README.zhTW.md` 之間 byte-identical，且 Open-in-Colab CTA hyperlink 引用同一個 `<current-tag>` 對應的 `notebooks/task_template.ipynb` URL。對應 `Cantus README ships a Traditional Chinese variant with bidirectional language switch` 的 Scenario「Install and Quickstart code blocks are byte-identical to README.md」。驗證：以 awk/sed 抽出兩檔之 ```bash 與 ```python 程式區塊後 `diff -u` 結果為空；對兩檔同時 grep `colab.research.google.com/github/schola-cantorum/cantus/blob/[^/]+/notebooks/task_template.ipynb` 取得的 `<tag>` 區段完全相同。
- [x] [P] 3.2 確認 `libs/cantus/README.md` 的「繁體中文」連結與 `libs/cantus/README.zhTW.md` 的「English」連結均使用 repo-relative href（不含 `http://`、`https://`、開頭斜線 `/`），可被 GitHub web rendering 與 Colab notebook rendering 雙邊解析。對應 `Cantus README ships a Traditional Chinese variant with bidirectional language switch` 的 Scenario「bidirectional language-switch hyperlinks resolve via repo-relative paths」。驗證：`grep -E '\[[^]]*繁體中文[^]]*\]\(README\.zhTW\.md\)' libs/cantus/README.md` 命中恰一次；`grep -E '\[[^]]*English[^]]*\]\(README\.md\)' libs/cantus/README.zhTW.md` 命中恰一次；兩條 href 字串均未出現 `http`、`https`、或開頭的 `/`。
