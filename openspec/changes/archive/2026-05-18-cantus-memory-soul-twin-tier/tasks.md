## 1. Turn dataclass 擴張

- [x] 1.1 [implements Requirement "Turn dataclass extends with optional timestamp and type fields"; honours Decision "`Turn` 擴張採向後相容欄位、不破壞 v0.3.0 ABI"] 擴張 `cantus.protocols.memory.Turn` 新增 optional `timestamp: datetime | None = None` 與 optional `type: Literal["user", "assistant", "system", "tool"] | None = None`，並實作 `__post_init__` 在 `type is None` 時依 user/assistant 內容推導；兩欄皆空時 raise `ValueError("empty Turn ...")`。Behavior：v0.3.0 既有 `Turn(user="...", assistant="...")` 仍可建構且 `timestamp is None`、`type` 從推導表得出。Verification：`uv run pytest libs/cantus/tests/test_memory.py::test_turn_v030_byte_identical libs/cantus/tests/test_memory.py::test_turn_type_derivation libs/cantus/tests/test_memory.py::test_turn_empty_raises libs/cantus/tests/test_memory.py::test_turn_whitespace_rejected libs/cantus/tests/test_memory.py::test_turn_type_literal_rejects_system_and_tool -v` 全綠（audit Trap-3 + Trap-7 fix：Literal 收窄為 `Literal["user", "assistant"]`、whitespace-only 拒絕、`type="system"` / `type="tool"` 拒絕）。

## 2. MarkdownMemory 底層實作

- [x] 2.1 [P] [implements Requirement "MarkdownMemory persists turns to a file with frontmatter" (path safety branch); honours Decision "`MarkdownMemory` 採 frontmatter + body 結構"; honours Decision "Memory module 拆分為 `memory.py` + `memory_markdown.py` + `memory_auto.py`"; honours Decision "Memory 維持 class-only entry，與 v0.3.0 既有 Requirement 對齊"] 在 `cantus.protocols.memory_markdown` 新增 `_validate_safe_path(path)` helper：拒絕含 `..` 跨上層、拒絕 resolve 後落在 `/etc` `/sys` `/proc` `/dev` `/root` 任一前綴；拒絕時 raise `ValueError` 訊息含 `"path traversal"` 或 `"system path"`。Behavior：safe path 通過、不安全 path 立刻 fail-loud 且不開檔。Verification：`uv run pytest libs/cantus/tests/test_memory_markdown.py::test_path_traversal_rejected libs/cantus/tests/test_memory_markdown.py::test_system_path_rejected libs/cantus/tests/test_memory_markdown.py::test_safe_path_accepted libs/cantus/tests/test_memory_markdown.py::test_symlink_to_system_path_rejected libs/cantus/tests/test_memory_markdown.py::test_fifo_socket_block_device_rejected libs/cantus/tests/test_memory_markdown.py::test_windows_unc_rejected -v` 全綠（audit Trap-1 fix：resolve-then-classify 流程；symlink、FIFO/socket/block-device、Windows UNC 全部拒絕）。
- [x] 2.2 [implements Requirement "MarkdownMemory persists turns to a file with frontmatter" (round-trip + missing-file branches); implements Requirement "Memory protocol exposes two layers with distinct entry points" (lower-tier MarkdownMemory branch); implements Requirement "Memory protocol exposes two API tiers" (lower-tier branch in agent-protocols delta)] 新增 `MarkdownMemory(path)` class subclassing `Memory`：建構子呼叫 `_validate_safe_path`；`remember(turn)` 將 `Turn` 序列化為 YAML frontmatter + body 並 append（含 `---` 分隔）；`recall(query)` 解析全檔回傳子字串符合的 `Turn` list；缺檔回 `[]`。Behavior：round-trip 一筆 Turn 後 `recall` 回得到原 Turn 內容；缺檔 `recall` 回 `[]` 不 raise；frontmatter 含 `timestamp` / `type` / `user` / `assistant` 四鍵。Verification：`uv run pytest libs/cantus/tests/test_memory_markdown.py::test_round_trip_one_turn libs/cantus/tests/test_memory_markdown.py::test_recall_missing_file_returns_empty libs/cantus/tests/test_memory_markdown.py::test_frontmatter_keys libs/cantus/tests/test_memory_markdown.py::test_recall_top_k_default_10 libs/cantus/tests/test_memory_markdown.py::test_recall_top_k_custom libs/cantus/tests/test_memory_markdown.py::test_recall_returns_in_file_order -v` 全綠（audit Trap-5 fix：`top_k` 預設 10 + file order + 可由建構子調整）。

## 3. cantus.identity Soul 模組

- [x] 3.1 [P] [implements Requirement "Soul class loads identity from a SOUL.md file"; honours Decision "`Soul.from_file()` 直接解析、不做 lossless round-trip"] 新增 `cantus.identity.SoulParseError(ValueError)` 帶 `path`、`missing_sections`、`duplicates` 三個 attribute；新增 `cantus.identity.Soul` dataclass 含六個 `name_and_role` / `personality` / `rules` / `tools` / `output_format` / `handoffs` 字串屬性；實作 `Soul.from_file(path)` 解析六個 `## ` H2 標頭。Behavior：合法 SOUL.md 載入後六屬性皆非空字串；缺 H2 → `SoulParseError(missing_sections=[...])`；重複 H2 → `SoulParseError(duplicates=[...])`；缺檔 → `FileNotFoundError`（不包裝）。Verification：`uv run pytest libs/cantus/tests/test_identity_soul.py::test_full_parse libs/cantus/tests/test_identity_soul.py::test_missing_sections libs/cantus/tests/test_identity_soul.py::test_duplicate_sections libs/cantus/tests/test_identity_soul.py::test_missing_file_raises_fnf libs/cantus/tests/test_identity_soul.py::test_wrong_case_header_reported_missing_plus_unexpected libs/cantus/tests/test_identity_soul.py::test_unrecognised_h2_rejected -v` 全綠（audit Trap-8 fix：case-sensitive 比對 + `unexpected` 屬性報告非規格 H2；依賴 fixture `libs/cantus/tests/fixtures/soul_full.md` / `soul_minimal.md` / `soul_missing_two.md` / `soul_duplicate_rules.md` / `soul_wrongcase_name.md` / `soul_extra_examples.md` 新建）。
- [x] 3.2 [implements Requirement "Soul renders to a stable system prompt string"] 實作 `Soul.to_system_prompt()` 按六 H2 順序與單空行分隔渲染；確定性（兩次呼叫 byte-identical）；in-memory `Soul.from_text(string)` parser 配合 round-trip 測試。Behavior：渲染輸出以 `## Name & Role` 開頭、無 leading newline；`from_text(soul.to_system_prompt())` 重建後六屬性 byte-identical。Verification：`uv run pytest libs/cantus/tests/test_identity_soul.py::test_to_system_prompt_deterministic libs/cantus/tests/test_identity_soul.py::test_to_system_prompt_round_trip -v` 全綠。

## 4. EventStream JSON-Lines persistence

- [x] 4.1 [P] [implements Requirement "EventStream JSON-Lines persistence"; honours Decision "EventStream 持久化採 JSON-Lines append-only、單檔、不分 segment"] 新增 `cantus.core.event_stream_persistence.JsonLinesPersistence(path)`：`append(event)` 以 `json.dumps` 序列化、append 一行 + `os.fsync(fd)`；non-serialisable 時 raise `TypeError("... not JSON serializable ...")` 且**不**寫半行（先 dumps 成功才開檔 append）。`load()` 缺檔回 `[]`；存在時逐行 `json.loads` 回 list。Behavior：round-trip append 後 load 回得原 event；缺檔 load 回 `[]` 不建檔；non-serialisable append 不污染檔案。Verification：`uv run pytest libs/cantus/tests/test_event_stream_persistence.py::test_append_load_round_trip libs/cantus/tests/test_event_stream_persistence.py::test_cold_start_empty libs/cantus/tests/test_event_stream_persistence.py::test_non_serialisable_no_partial_write libs/cantus/tests/test_event_stream_persistence.py::test_fsync_called libs/cantus/tests/test_event_stream_persistence.py::test_file_mode_0600 libs/cantus/tests/test_event_stream_persistence.py::test_serialise_failure_does_not_create_file -v` 全綠（`test_fsync_called` 用 `unittest.mock.patch("os.fsync")` 驗證每次 append 都呼叫 fsync；audit Trap-6 fix：`test_file_mode_0600` 驗 POSIX `stat.S_IMODE` == `0o600`、`test_serialise_failure_does_not_create_file` 驗 dumps 失敗時 cold-start path 不被建檔）。

## 5. AutoMemory 高階 LLM-facing 介面

- [x] 5.1 [implements Requirement "AutoMemory tool spec_for_llm honours v0.3.0 skill shape contract"; implements Requirement "Memory protocol exposes two layers with distinct entry points" (upper-tier AutoMemory branch); implements Requirement "Memory protocol exposes two API tiers" (upper-tier branch in agent-protocols delta); honours Decision "`AutoMemory` 暴露 4 tool 而非單一 `auto_recall` skill"; honours Decision "`AutoMemory(backend=...)` 採 composition 而非 inheritance"] 在 `cantus.protocols.memory_auto` 新增 `AutoMemory(backend: Memory)`：建構子接受任一 `Memory` 實例；`tools` property 回 list 含 4 個 `Skill` 實例，名稱依序 `view` / `create` / `str_replace` / `delete`；4 個 Skill 內部 closure 持有 `backend` 但不洩漏進 `spec_for_llm()`。Behavior：`AutoMemory` 非 `Memory` 子類；4 tool 為 `Skill` 實例；每 tool `spec_for_llm()` 回傳 dict top-level keys 恰為 `{"name", "description", "args_schema"}`；無 `backend` / `_pre_hook` / `_post_hook` 等內部鍵洩漏。Verification：`uv run pytest libs/cantus/tests/test_memory_auto.py::test_tools_length_and_names libs/cantus/tests/test_memory_auto.py::test_auto_not_subclass_of_memory libs/cantus/tests/test_memory_auto.py::test_each_tool_spec_for_llm_shape libs/cantus/tests/test_memory_auto.py::test_create_invokes_backend_remember libs/cantus/tests/test_memory_auto.py::test_view_invokes_backend_recall libs/cantus/tests/test_memory_auto.py::test_tools_property_is_cached libs/cantus/tests/test_memory_auto.py::test_tools_docstring_contains_warning -v` 全綠（audit Trap-4 + Trap-10 fix：`tools` property 多次存取回 identical list object、docstring 含 `"LLM has full CRUD access"` 字面警告）。

## 6. Agent.soul= 系統 prompt 注入

- [x] 6.1 [implements Requirement "Agent accepts soul keyword for system prompt injection"; implements Requirement "Soul identity injects into Agent system prompt"] 修改 `cantus.core.agent.Agent.__init__` 增加 keyword-only `soul: Soul | None = None`。當 `soul is None` 時系統 prompt 構建與 v0.3.0 byte-identical；當 `soul` 為 `Soul` 實例時系統 prompt 以 `soul.to_system_prompt() + "\n\n"` 為前綴、其餘部分與 v0.3.0 baseline byte-identical。`soul` **不**註冊為 Skill、**不**出現在 `registry.spec_for_llm()`。Behavior：兩條 scenario 都可機械驗證 system prompt 字串；無 soul 路徑與 v0.3.0 完全相同。Verification：`uv run pytest libs/cantus/tests/test_agent.py::test_no_soul_baseline_byte_identical libs/cantus/tests/test_agent.py::test_soul_prefix_injection libs/cantus/tests/test_agent.py::test_soul_not_in_registry_spec_for_llm -v` 全綠。

## 7. 對外 API exports 與公開介面測試

- [x] 7.1 更新 `cantus/__init__.py`：自子模組 re-export `MarkdownMemory`、`AutoMemory` from `cantus.protocols.memory`；export `Soul`、`SoulParseError` from `cantus.identity`；export `JsonLinesPersistence` from `cantus.core.event_stream_persistence`。更新 `__all__` 對齊。Behavior：`from cantus import MarkdownMemory, AutoMemory` 與 `from cantus.identity import Soul, SoulParseError` 與 `from cantus.core.event_stream_persistence import JsonLinesPersistence` 全綠；既有 v0.3.0 import 不變。Verification：`uv run pytest libs/cantus/tests/test_public_api.py -v` 全綠（含 v0.3.1 新名稱加入 `__all__` 比對與 v0.3.0 既有名稱保留比對）。

## 8. 版本 bump 與 CHANGELOG

- [x] 8.1 `libs/cantus/pyproject.toml` 將 `version = "0.3.0"` 改為 `version = "0.3.1"`；`libs/cantus/cantus/__init__.py` 內 `__version__ = "0.3.1"`（如有）。新增 `libs/cantus/CHANGELOG.md` `## [0.3.1] - 2026-MM-DD` 段，列 added（MarkdownMemory / AutoMemory / Soul / SoulParseError / JsonLinesPersistence / Agent.soul= / Turn extra fields），標註「PATCH-equivalent additive，無 BREAKING」。Behavior：`python -c "import cantus; assert cantus.__version__ == '0.3.1'"` 通過；`grep '## \[0.3.1\]' libs/cantus/CHANGELOG.md` 命中。Verification：上述兩條 shell 指令皆 exit code 0。

## 9. 文件與 migration guide

- [x] 9.1 更新 `libs/cantus/docs/protocols/memory.md`：增「雙層 API」段，列 4 個底層 + `AutoMemory` 雙層；附 `MarkdownMemory` path safety 規則；附 `EventStream` JSON-Lines persistence 用法範例；明示「`AutoMemory` LLM 自主 CRUD 權限，正式應用前可用 `@skill(post_hook=...) wrap` 過濾」。Verification：`uv run python scripts/wiki_validator.py libs/cantus/docs/llm_wiki/` 零違規（既有 wiki-validator hook）；human review 確認雙層段、path safety 規則、persistence 範例皆存在。
- [x] 9.2 新增 `libs/cantus/docs/protocols/identity.md`：列 SOUL.md 六區塊規格（對齊 aaronjmars/soul.md）、`Soul.from_file()` 失敗模式、`Agent(soul=...)` 注入順序、override pattern（`soul=None` + 自塞 system prompt）。Verification：`uv run python scripts/wiki_validator.py libs/cantus/docs/llm_wiki/` 零違規；human review 確認六區塊、失敗模式、注入順序、override pattern 四段皆存在。
- [x] 9.3 新增 `libs/cantus/MIGRATION_v0.3_to_v0.3.1.md`：含 4 段：（a）Turn 擴張欄位 opt-in 引導；（b）`AutoMemory` 採用範例（搭配 `Agent.run`）；（c）`Soul` 與 `Agent(soul=...)` 採用步驟；（d）`JsonLinesPersistence` cross-session reload pattern。Verification：human review 確認 4 段皆有可貼上即跑的 Python 範例片段。

## 10. 整合 verification（apply 完成 gate）

- [x] 10.1 `uv run pytest libs/cantus/tests/ -v` 整體綠（v0.3.0 既有測試 + v0.3.1 新測試聯合）。
- [x] 10.2 `uv run ruff check libs/cantus/` 與 `uv run mypy libs/cantus/cantus/` 兩者皆零錯誤。
- [x] 10.3 `jupyter nbconvert --to notebook --execute --inplace libs/cantus/notebooks/task_template.ipynb` 跑完不 raise（既有 notebook 仍能在 v0.3.1 環境執行）。
- [x] 10.4 `spectra verify cantus-memory-soul-twin-tier` 與 `spectra audit cantus-memory-soul-twin-tier` 兩條命令皆無 Critical / Warning（Suggestion 可保留）。
