## 1. cantus.adapters 套件骨架

- [x] 1.1 [implements Requirement "cantus.adapters package exposes three top-level functions"; honours Decision "`cantus.adapters` 採函式式公開介面而非 adapter base class"; honours Decision "`cantus[mcp]` 為唯一新 extras、不開 `cantus[adapters]` 集成 extras"] 新建 `libs/cantus/cantus/adapters/__init__.py` 並 export 三個 callable stub：`export_as_mcp_server` / `import_mcp_server` / `expose_as_anthropic_memory_tool`（先 stub 為 `raise NotImplementedError`）；確認 `Registry.KINDS` 不變、registry 未被 mutate；`cantus.adapters` 不引入新 protocol kind。Behavior：`from cantus.adapters import export_as_mcp_server, import_mcp_server, expose_as_anthropic_memory_tool` 三 callable 皆可 import；`cantus.core.registry.Registry().KINDS == ("skill",)` 保持。Verification：`uv run pytest libs/cantus/tests/adapters/test_adapters_package.py::test_three_public_functions_importable libs/cantus/tests/adapters/test_adapters_package.py::test_registry_kinds_unchanged_after_import -v` 全綠。

## 2. Anthropic Memory adapter（純函式優先做）

- [x] 2.1 [implements Requirement "expose_as_anthropic_memory_tool returns a JSON-serialisable dict"; honours Decision "Anthropic Memory adapter 回傳純 dict、不持有 Memory 實例引用"] 新建 `libs/cantus/cantus/adapters/anthropic_memory.py` 實作 `expose_as_anthropic_memory_tool(memory)`：accept `Memory | AutoMemory`、reject 其他型別（`TypeError("expects Memory or AutoMemory ...")`）；回傳 dict top-level keys `{type, name, description, commands}`，`commands` 含恰 4 鍵 `{view, create, str_replace, delete}`、每 command 為 `{description, args_schema}` 兩鍵；無 callable / Memory instance 引用；`json.dumps(result)` 不 raise。更新 `cantus/adapters/__init__.py` re-export。Behavior：上述 shape 與 json round-trip 可機械驗證。Verification：`uv run pytest libs/cantus/tests/adapters/test_anthropic_memory.py::test_dict_shape libs/cantus/tests/adapters/test_anthropic_memory.py::test_four_command_keys libs/cantus/tests/adapters/test_anthropic_memory.py::test_json_dumps_succeeds libs/cantus/tests/adapters/test_anthropic_memory.py::test_rejects_non_memory_input -v` 全綠。

## 3. MCP server adapter

- [x] 3.1 [implements Requirement "export_as_mcp_server wraps cantus Skills as MCP tools"; honours Decision "MCP server 採 `mcp` 官方 Python SDK、非自寫 JSON-RPC"; honours Decision "MCP tool inputSchema 由 `Skill.spec_for_llm()["args_schema"]` 直接帶入、不轉換"; honours Decision "MCP transport 限定 stdio + streamable_http、不做 SSE"; honours Decision "MCP spec revision target 鎖定 2025-11-25"] 新建 `libs/cantus/cantus/adapters/mcp.py`（lazy import 包 + `ImportError("... pip install cantus[mcp]")`），與 `libs/cantus/cantus/adapters/mcp_server.py` 實作 `McpServer` class + `export_as_mcp_server(skills, name, version)`：empty list → `ValueError("requires at least one Skill ...")`；non-Skill 元素 → `TypeError("expects list[Skill] ...")`；`McpServer.tools` 為 `list[dict]` 每個 dict 三鍵 `{name, description, inputSchema}` 直接從 `spec_for_llm()` 帶入；`McpServer.run(transport=...)` 接受 `"stdio"` / `"http"` 兩值、其他 `ValueError("transport must be 'stdio' or 'http' ...")`；`run()` 阻塞執行。Verification：`uv run pytest libs/cantus/tests/adapters/test_mcp_server.py::test_export_preserves_spec_for_llm_shape libs/cantus/tests/adapters/test_mcp_server.py::test_export_rejects_empty_list libs/cantus/tests/adapters/test_mcp_server.py::test_export_rejects_non_skill libs/cantus/tests/adapters/test_mcp_server.py::test_run_rejects_unsupported_transport libs/cantus/tests/adapters/test_mcp_server.py::test_import_without_mcp_sdk_raises_actionable_error libs/cantus/tests/adapters/test_mcp_server.py::test_export_rejects_invalid_name_or_version libs/cantus/tests/adapters/test_mcp_server.py::test_run_raises_address_in_use_when_port_busy -v` 全綠（最後一條的 fixture 啟動兩個 server 對同 port；前者用 monkeypatch 模擬 mcp SDK 缺席；新增 `test_export_rejects_invalid_name_or_version` 對應 audit Trap-4 修正、`test_run_raises_address_in_use_when_port_busy` 對應 audit Trap-2 修正）。

## 4. MCP client adapter

- [x] 4.1 [implements Requirement "import_mcp_server wraps remote MCP tools as cantus Skills"; implements Requirement "cantus.adapters error naming convention"; honours Decision "MCP client 把每個 MCP tool 包成 v0.3.0-shape 的 cantus `Skill`"] 新建 `libs/cantus/cantus/adapters/mcp_client.py` 實作 `import_mcp_server(transport, command_or_url) -> list[Skill]`：每個 MCP tool 包成 cantus Skill 實例，`spec_for_llm()` top-level keys 恰為 `{"name", "description", "args_schema"}`，`args_schema` 直接是 MCP tool 的 `inputSchema`，Skill body 為 closure 呼叫 SDK `tools/call`、回傳值原樣 forward；handshake 失敗 → `RuntimeError("mcp_handshake_failed ...")`；remote tool call 失敗 → cantus `ToolErrorObservation(message="mcp_remote_error: ...")`。測試完全用 mocked SDK，無需實際 MCP server。Behavior：所有 imported Skill 的 `spec_for_llm()` shape 對齊 v0.3.0；handshake / remote error 路徑都有明確子字串。Verification：`uv run pytest libs/cantus/tests/adapters/test_mcp_client.py::test_import_returns_v030_shaped_skills libs/cantus/tests/adapters/test_mcp_client.py::test_imported_skill_args_schema_from_input_schema libs/cantus/tests/adapters/test_mcp_client.py::test_handshake_failure_raises_runtime_error libs/cantus/tests/adapters/test_mcp_client.py::test_remote_call_error_wraps_as_tool_error_observation libs/cantus/tests/adapters/test_mcp_client.py::test_rejects_shell_metacharacter_command libs/cantus/tests/adapters/test_mcp_client.py::test_rejects_non_http_url libs/cantus/tests/adapters/test_mcp_client.py::test_imported_skill_has_is_remote_attribute -v` 全綠（audit Trap-1 fix：shell metacharacter rejection；audit Trap-5 fix：`is_remote` provenance marker、且不洩漏進 `spec_for_llm()`；URL validation）。

## 5. Skill.spec_for_llm 不變回證（adapter 端雙層保險）

- [x] 5.1 [P] [implements Requirement "Adapter import preserves Skill.spec_for_llm JSON shape invariant"; implements Requirement "cantus.adapters preserves Skill.spec_for_llm JSON shape" (in agent-protocols delta)] 新建 `libs/cantus/tests/adapters/test_skill_spec_for_llm_invariant.py`：定義一個 `@skill def f(x: int) -> str` skill、capture `before = f.spec_for_llm()`、import `cantus.adapters.mcp_server`、`cantus.adapters.mcp_client`、`cantus.adapters.anthropic_memory` 三模組（用 monkeypatch 模擬 mcp SDK 存在）、capture `after = f.spec_for_llm()`、assert `before == after` deep equality；另一條測試對含 hook 的 skill 做相同驗證（與 v0.3.0 `test_spec_for_llm_shape_unchanged_with_hooks` 平行）。Verification：`uv run pytest libs/cantus/tests/adapters/test_skill_spec_for_llm_invariant.py -v` 全綠。

## 6. 對外 API exports 與公開介面測試

- [x] 6.1 更新 `cantus/__init__.py`：可選地 lazy export `export_as_mcp_server` / `import_mcp_server` / `expose_as_anthropic_memory_tool`（不強制 top-level；正規入口仍為 `from cantus.adapters import ...`）。更新 `__all__` 對齊。Behavior：v0.3.0 + v0.3.1 既有 import 全部不變；新增三 adapter 函式皆可由 `cantus.adapters` 取得。Verification：`uv run pytest libs/cantus/tests/test_public_api.py -v` 全綠（含 v0.3.2 新名稱比對與 v0.3.0 / v0.3.1 既有名稱保留比對）。

## 7. 版本 bump 與 CHANGELOG

- [x] 7.1 [removes Requirement "Distribution extras matrix exposes openai, anthropic, google, groq, providers, and dev groups"; implements Requirement "Distribution extras matrix exposes openai, anthropic, google, groq, providers, mcp, and dev groups"] `libs/cantus/pyproject.toml` 將 `version = "0.3.1"` 改為 `version = "0.3.2"`；新增 `[mcp]` 至 `[project.optional-dependencies]`（`mcp>=0.1,<2`）；`libs/cantus/cantus/__init__.py` 內 `__version__ = "0.3.2"`。新增 `libs/cantus/CHANGELOG.md` `## [0.3.2] - 2026-MM-DD` 段，列 added（`cantus.adapters` 套件 + 三函式 + `cantus[mcp]` extras），標註「MINOR additive，無 BREAKING」；既有 6 個 extras（`openai` / `anthropic` / `google` / `groq` / `providers` / `dev`）pin range 與 scenarios 全部保留 byte-identical（REMOVED 段的 Migration 明示）。Behavior：`python -c "import cantus; assert cantus.__version__ == '0.3.2'"` 通過；`grep '## \[0.3.2\]' libs/cantus/CHANGELOG.md` 命中；`pip install cantus[mcp]` 可在 dry-run 環境下解析 `mcp>=0.1,<2` 依賴；`pip install cantus[openai]` / `[anthropic]` / `[google]` / `[groq]` / `[providers]` / `[dev]` 全部仍可正常 resolve（既有 v0.3.1 行為不破）。Verification：上述 shell 指令皆 exit code 0；`uv pip install --dry-run 'cantus[mcp]'`（在 libs/cantus/ 目錄）成功；`uv pip install --dry-run 'cantus[providers]'` 仍成功且不解析 `mcp` 套件。

## 8. 文件與 migration guide

- [x] 8.1 新增 `libs/cantus/docs/protocols/adapters.md`：8 段結構（套件總覽、`expose_as_anthropic_memory_tool` 5-line tool_use 範例 + LLM CRUD foot-gun 警告（cross-reference v0.3.1 AutoMemory Trap-10）、`export_as_mcp_server` 5-line stdio 範例 + `port=0` 與 threading 範例（audit Trap-9 fix）、`import_mcp_server` 5-line stdio 範例 + 信任邊界（不要餵 untrusted command_or_url）、Schema Compatibility 段（解釋 Pydantic `extra="forbid"` 對 MCP/Anthropic API 的影響，audit Trap-7 fix）、Authorization & Memory Mutation 段（audit Trap-10 fix 明文 carry-over）、Error naming convention 段（audit Trap-8 fix）、v0.3.3 預告）。Verification：human review 確認 8 段皆有可貼上即跑的 Python 範例；`uv run python scripts/wiki_validator.py libs/cantus/docs/llm_wiki/` 零違規（若 docs/protocols/adapters.md 被 wiki-validator 掃描）。
- [x] 8.2 新增 `libs/cantus/MIGRATION_v0.3_to_v0.3.2.md`：含 3 段：（a）`expose_as_anthropic_memory_tool` 採用引導；（b）`export_as_mcp_server` + Claude Desktop 連接步驟；（c）`import_mcp_server` 採用引導與信任邊界。Verification：human review 確認 3 段皆有可貼上即跑的 Python 範例。

## 9. 整合 verification（apply 完成 gate）

- [x] 9.1 `uv run pytest libs/cantus/tests/ -v` 整體綠（v0.3.0 + v0.3.1 + v0.3.2 聯合通過）。
- [x] 9.2 `uv run ruff check libs/cantus/` 與 `uv run mypy libs/cantus/cantus/` 兩者皆零錯誤。
- [x] 9.3 `uv run pytest libs/cantus/tests/test_skill.py::test_spec_for_llm_shape_unchanged libs/cantus/tests/test_skill.py::test_spec_for_llm_shape_unchanged_with_hooks -v` 綠（v0.3.0 既有 contract 不破，adapter 落地後雙層保險仍工作）。
- [x] 9.4 `spectra verify cantus-adapter-layer` 與 `spectra audit cantus-adapter-layer` 兩條命令皆無 Critical / Warning（Suggestion 可保留）。
