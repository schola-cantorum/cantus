## 1. `_RemoteSkillBase` 共用基底抽離

- [x] 1.1 [implements Requirement "_RemoteSkillBase is the shared base for all import_* adapters"; honours Decision "抽離 `_RemoteSkillBase` 為共用基底"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 新建 `libs/cantus/cantus/adapters/_remote_skill.py` 內 `_RemoteSkillBase(Skill)` 類別：`is_remote = True` 類別屬性；`__init__(*, name, description, args_schema_dict)` 繞過 `Skill.__init__` signature introspection、直接設 `self.name` / `self.description` / `self._args_schema_dict` / `self._pre_hook = None` / `self._post_hook = None`；`spec_for_llm()` 回 `{"name", "description", "args_schema"}` 三鍵 dict；`validate_args(args)` 對非 dict 拋 `TypeError("remote adapter tool args must be a dict, got ...")`、否則回 `dict(args)`；`run()` 預設 raise `NotImplementedError("subclass must implement run() ...")`。新建 `libs/cantus/tests/adapters/test_remote_skill_base.py` 驗證上述五條。Behavior：concrete subclass 實例化後 `spec_for_llm()` 三鍵正確、`is_remote == True`、`is_remote` 不在 `spec_for_llm()` dict 內、`run()` 直接 raise NotImplementedError。Verification：`uv run pytest libs/cantus/tests/adapters/test_remote_skill_base.py::test_spec_for_llm_shape libs/cantus/tests/adapters/test_remote_skill_base.py::test_is_remote_attribute_not_in_spec libs/cantus/tests/adapters/test_remote_skill_base.py::test_validate_args_dict_only libs/cantus/tests/adapters/test_remote_skill_base.py::test_run_raises_not_implemented_in_base -v` 全綠。

- [x] 1.2 [implements Requirement "_RemoteSkillBase is the shared base for all import_* adapters" (v0.3.2 refactor branch)] 修改 `libs/cantus/cantus/adapters/mcp_client.py`：`_RemoteSkill` 改繼承 `_RemoteSkillBase`，`__init__` 呼叫 `super().__init__(name=tool_name, description=description, args_schema_dict=input_schema)` 後額外設 `self._transport` 與 `self._command_or_url`；保留既有 `run()` 邏輯（呼叫 `_call_remote_tool` + 失敗時 wrap `RuntimeError("mcp_remote_error: ...")`）；移除 `_RemoteSkill` 自己重複的 `spec_for_llm()` / `validate_args()`（改由 base 繼承）。Behavior：v0.3.2 `tests/adapters/test_mcp_client.py` 全部 9 條測試在 refactor 後仍綠（觀察行為 byte-identical）。Verification：`uv run pytest libs/cantus/tests/adapters/test_mcp_client.py -v` 全綠（v0.3.2 既有測試不增不減）。

## 2. LangChain adapter

- [x] 2.1 [implements Requirement "Batch2 SDK gates raise actionable ImportError when extras are missing" (langchain branch); honours Decision "各 adapter 模組 SDK gate 設計"] 新建 `libs/cantus/cantus/adapters/langchain.py`：top-level `try: import langchain_core` + `except ImportError as exc: raise ImportError("cantus.adapters.langchain requires the langchain-core SDK. Run: pip install cantus[langchain]") from exc`。**測試端**用 `monkeypatch.setitem(sys.modules, "langchain_core", None)` 模擬 SDK 缺席、走 fresh re-import 路徑（沿用 v0.3.2 `test_mcp_server.py::test_import_without_mcp_sdk_raises_actionable_error` 模式）。Behavior：缺 SDK 時 ImportError 訊息含子字串 `"pip install cantus[langchain]"`。Verification：`uv run pytest libs/cantus/tests/adapters/test_langchain.py::test_import_without_langchain_sdk_raises_actionable_error -v` 綠。

- [x] 2.2 [P] [implements Requirement "expose_as_langchain_tool produces a v0.3.0-shape-compatible LangChain Tool"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 在 `libs/cantus/cantus/adapters/langchain.py` 實作 `expose_as_langchain_tool(skill: Skill) -> BaseTool`：non-Skill input → `TypeError("expose_as_langchain_tool expects Skill, got <type>")`；從 `skill.spec_for_llm()` 三鍵建構動態 Pydantic v2 model（用 `pydantic.create_model` + JSON Schema → field annotation 轉換）作為 LangChain Tool 的 `args_schema`；回傳的 LangChain BaseTool 子類實例 `name` / `description` / `args_schema.model_json_schema()` 三欄對齊 Skill 三鍵。**測試端**用 mock LangChain SDK fixture（注入 fake `BaseTool` class 接受 `name` / `description` / `args_schema` keyword）。Verification：`uv run pytest libs/cantus/tests/adapters/test_langchain.py::test_expose_round_trip libs/cantus/tests/adapters/test_langchain.py::test_expose_rejects_non_skill -v` 綠。

- [x] 2.3 [P] [implements Requirement "import_langchain_tool wraps LangChain BaseTool as cantus Skill"; honours Decision "LangChain / DSPy `import_*` 的 schema 轉換策略"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 在 `libs/cantus/cantus/adapters/langchain.py` 實作 `import_langchain_tool(tool: BaseTool) -> Skill`：non-BaseTool input → `TypeError("import_langchain_tool expects langchain_core.tools.BaseTool, got <type>")`；建立 `_LangChainRemoteSkill(_RemoteSkillBase)` 子類，`__init__` 呼叫 `super().__init__(name=tool.name, description=tool.description or "", args_schema_dict=<derived>)`，其中 `<derived>` 為 `tool.args_schema.model_json_schema()`（當 `tool.args_schema` 是 Pydantic v2 model）或 `{"type": "object", "properties": {}, "required": []}`（當 `args_schema is None`）；`tool.args_schema` 非 None 但無法 `model_json_schema()` 時 → `RuntimeError("langchain_handshake_failed: ...")`；`run(**kwargs)` 走 `tool.invoke(kwargs)`、失敗時 wrap `RuntimeError("langchain_remote_error: ...")`。Verification：`uv run pytest libs/cantus/tests/adapters/test_langchain.py::test_import_returns_v030_shaped_skill libs/cantus/tests/adapters/test_langchain.py::test_import_with_none_args_schema libs/cantus/tests/adapters/test_langchain.py::test_import_handshake_failure libs/cantus/tests/adapters/test_langchain.py::test_import_rejects_non_basetool libs/cantus/tests/adapters/test_langchain.py::test_imported_skill_is_remote_marker -v` 全綠。

## 3. DSPy adapter

- [x] 3.1 [implements Requirement "Batch2 SDK gates raise actionable ImportError when extras are missing" (dspy branch); honours Decision "各 adapter 模組 SDK gate 設計"] 新建 `libs/cantus/cantus/adapters/dspy.py`：top-level `try: import dspy` + `ImportError("cantus.adapters.dspy requires the dspy-ai SDK. Run: pip install cantus[dspy]")` from exc。**測試端**同 langchain 模式 monkeypatch。Verification：`uv run pytest libs/cantus/tests/adapters/test_dspy.py::test_import_without_dspy_sdk_raises_actionable_error -v` 綠。

- [x] 3.2 [P] [implements Requirement "expose_as_dspy_tool produces a DSPy Tool from a cantus Skill"; honours Decision "LangChain / DSPy `import_*` 的 schema 轉換策略"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 在 `libs/cantus/cantus/adapters/dspy.py` 實作 `expose_as_dspy_tool(skill: Skill) -> dspy.Tool`：non-Skill → `TypeError("expose_as_dspy_tool expects Skill, got <type>")`；從 `skill.spec_for_llm()["args_schema"]["properties"]` 逐 property 構建 DSPy Tool input signature（type mapping：`"string"→str`、`"integer"→int`、`"number"→float`、`"boolean"→bool`，其他 fall back `str`）；DSPy Tool `name` / `desc` 對齊 Skill 兩鍵。**測試端**用 mock DSPy SDK fixture。Verification：`uv run pytest libs/cantus/tests/adapters/test_dspy.py::test_expose_round_trip libs/cantus/tests/adapters/test_dspy.py::test_expose_rejects_non_skill libs/cantus/tests/adapters/test_dspy.py::test_expose_type_mapping -v` 全綠。

- [x] 3.3 [P] [implements Requirement "import_dspy_tool wraps DSPy Tool as cantus Skill"; honours Decision "LangChain / DSPy `import_*` 的 schema 轉換策略"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 在 `libs/cantus/cantus/adapters/dspy.py` 實作 `import_dspy_tool(tool: dspy.Tool) -> Skill`：non-Tool input → `TypeError("import_dspy_tool expects dspy.Tool, got <type>")`；建立 `_DspyRemoteSkill(_RemoteSkillBase)`，`args_schema_dict` 從 `tool.signature.input_fields` 逐 field 構建 JSON Schema（用 inverse type mapping `str→"string"`、`int→"integer"`、`float→"number"`、`bool→"boolean"`，其他 fall back `"string"`；required = 非 optional fields）；`run(**kwargs)` 走 `tool(...)` 或 SDK 建議 dispatch、失敗時 wrap `RuntimeError("dspy_remote_error: ...")`；signature 解析失敗 → `RuntimeError("dspy_handshake_failed: ...")`。Verification：`uv run pytest libs/cantus/tests/adapters/test_dspy.py::test_import_returns_v030_shaped_skill libs/cantus/tests/adapters/test_dspy.py::test_import_handshake_failure libs/cantus/tests/adapters/test_dspy.py::test_import_rejects_non_tool libs/cantus/tests/adapters/test_dspy.py::test_imported_skill_is_remote_marker -v` 全綠。

## 4. HuggingFace adapter

- [x] 4.1 [implements Requirement "Batch2 SDK gates raise actionable ImportError when extras are missing" (huggingface branch); implements Requirement "expose_as_hf_tool produces a HuggingFace transformers Tool from a cantus Skill"; honours Decision "各 adapter 模組 SDK gate 設計"; honours Decision "HuggingFace import 方向延 v0.3.4"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 新建 `libs/cantus/cantus/adapters/huggingface.py`：top-level `try: from transformers import Tool` + `ImportError("cantus.adapters.huggingface requires the transformers SDK. Run: pip install cantus[huggingface]")` from exc；實作 `expose_as_hf_tool(skill: Skill) -> transformers.Tool`：non-Skill → `TypeError("expose_as_hf_tool expects Skill, got <type>")`；`name` / `description` 對齊 Skill 兩鍵；`inputs` dict 由 `skill.spec_for_llm()["args_schema"]["properties"]` 逐 property 構建 `{type: <json schema type str>, description: <property's description or empty>}`；不引入 `import_hf_tool`（v0.3.4 batch3 評估）。**測試端**用 mock `transformers.Tool` class fixture。Verification：`uv run pytest libs/cantus/tests/adapters/test_huggingface.py::test_import_without_transformers_sdk_raises_actionable_error libs/cantus/tests/adapters/test_huggingface.py::test_expose_round_trip libs/cantus/tests/adapters/test_huggingface.py::test_expose_rejects_non_skill libs/cantus/tests/adapters/test_huggingface.py::test_import_hf_tool_not_exported -v` 全綠（最後一條驗證 `from cantus.adapters.huggingface import import_hf_tool` 拋 ImportError）。

## 5. OpenHands adapter

- [x] 5.1 [implements Requirement "Batch2 SDK gates raise actionable ImportError when extras are missing" (openhands branch); implements Requirement "expose_as_openhands_action produces an OpenHands Action from a cantus Skill"; honours Decision "OpenHands pin `openhands>=1.16,<2`（主套件而非 sdk 子套件）"; honours Decision "各 adapter 模組 SDK gate 設計"; honours Decision "Input validation 沿用 v0.3.2 audit fix 模板"] 新建 `libs/cantus/cantus/adapters/openhands.py`：top-level `try: from openhands.events import Action` + `ImportError("cantus.adapters.openhands requires the openhands SDK. Run: pip install cantus[openhands]")` from exc；實作 `expose_as_openhands_action(skill: Skill) -> Action`：non-Skill → `TypeError("expose_as_openhands_action expects Skill, got <type>")`；回傳通用 `openhands.events.Action` base instance、identifying 欄位（如 `tool_name` 或 SDK 對應屬性）由 Skill name / description 帶入；不引入 `import_openhands_action`（v0.3.4 batch3 評估）。**測試端**用 mock `openhands.events.Action` class fixture（注入 attribute-accepting fake）。Verification：`uv run pytest libs/cantus/tests/adapters/test_openhands.py::test_import_without_openhands_sdk_raises_actionable_error libs/cantus/tests/adapters/test_openhands.py::test_expose_round_trip libs/cantus/tests/adapters/test_openhands.py::test_expose_rejects_non_skill libs/cantus/tests/adapters/test_openhands.py::test_import_openhands_action_not_exported -v` 全綠。

## 6. 對外 API exports 與公開介面測試

- [x] 6.1 [implements Requirement "cantus.adapters.batch2 package exposes six callables across four frameworks"; honours Decision "不為 batch2 新增 `agent-protocols` delta"] 更新 `libs/cantus/cantus/adapters/__init__.py`：新增 6 個 lazy import stub（沿用 v0.3.2 既有 3 個 stub 模式 — 每個 stub 內部 `from cantus.adapters.<module> import <callable> as _impl; return _impl(*args, **kwargs)`）；更新 `__all__` 加 6 個新名稱（既有 3 個 v0.3.2 名稱不動）。擴張 `libs/cantus/tests/adapters/test_adapters_package.py` 加新測試：v0.3.3 9 個 callable（v0.3.2 3 個 + v0.3.3 6 個）皆可由 `from cantus.adapters import ...` 取得；`Registry.KINDS == ("skill",)` 不變。Behavior：v0.3.0 / v0.3.1 / v0.3.2 既有 import 全部 byte-identical 保留。Verification：`uv run pytest libs/cantus/tests/adapters/test_adapters_package.py -v` 全綠（含 v0.3.3 新名稱 + v0.3.2 / v0.3.1 / v0.3.0 既有名稱保留比對）。

- [x] 6.2 [implements Requirement "Batch2 adapters preserve Skill.spec_for_llm JSON shape invariant"] 擴張 `libs/cantus/tests/adapters/test_skill_spec_for_llm_invariant.py`：除既有 v0.3.2 三模組（`cantus.adapters.mcp_server`、`mcp_client`、`anthropic_memory`）外，再加 import `cantus.adapters.langchain` / `dspy` / `huggingface` / `openhands` / `_remote_skill` 五個 v0.3.3 新模組（用 monkeypatch 注入 fake SDK 讓四個 framework gate 不直接拋 ImportError）；驗證 `Skill.spec_for_llm()` shape 在 import 9 個模組後仍 byte-identical。Verification：`uv run pytest libs/cantus/tests/adapters/test_skill_spec_for_llm_invariant.py -v` 全綠。

## 7. 版本 bump 與 CHANGELOG / extras matrix 擴張

- [x] 7.1 [removes Requirement "Distribution extras matrix exposes openai, anthropic, google, groq, providers, mcp, and dev groups"; implements Requirement "Distribution extras matrix exposes openai, anthropic, google, groq, providers, mcp, langchain, dspy, huggingface, openhands, and dev groups"; honours Decision "LangChain pin `langchain-core` 而非 `langchain` 主套件"; honours Decision "DSPy pin `dspy-ai>=2.5,<3`"; honours Decision "HuggingFace pin `transformers>=4.40,<5`"; honours Decision "OpenHands pin `openhands>=1.16,<2`（主套件而非 sdk 子套件）"] `libs/cantus/pyproject.toml` 將 `version = "0.3.2"` 改為 `version = "0.3.3"`；新增 4 個 `[project.optional-dependencies]` key：`langchain = ["langchain-core>=0.3,<1"]`、`dspy = ["dspy-ai>=2.5,<3"]`、`huggingface = ["transformers>=4.40,<5"]`、`openhands = ["openhands>=1.16,<2"]`；既有 9 個 extras（`runtime` / `memory` / `openai` / `anthropic` / `google` / `groq` / `mcp` / `providers` / `dev`）pin range 與順序 byte-identical 保留。`libs/cantus/cantus/__init__.py` 內 `__version__ = "0.3.3"`。新增 `libs/cantus/CHANGELOG.md` `## [0.3.3] - 2026-MM-DD` 段，列 added（6 callable + `_RemoteSkillBase` + 4 個 extras + `mcp_client._RemoteSkill` refactor），標註「MINOR additive，無 BREAKING」。更新 `libs/cantus/tests/test_public_api.py` 加 v0.3.3 斷言（`cantus.__version__ == "0.3.3"` + 6 個 callable importable + 4 個新 gate 模組缺 SDK 時拋 ImportError）。Verification：`python -c "import cantus; assert cantus.__version__ == '0.3.3'"` 通過；`grep '## \[0.3.3\]' libs/cantus/CHANGELOG.md` 命中；`uv pip install --dry-run 'cantus[langchain]'` / `[dspy]` / `[huggingface]` / `[openhands]` 在 libs/cantus/ 目錄全部成功且 resolve 對應套件；`uv pip install --dry-run 'cantus[providers]'` 仍成功且不解析 4 個 batch2 套件；`uv run pytest libs/cantus/tests/test_public_api.py -v` 全綠。

## 8. 文件與 migration guide

- [x] 8.1 [honours Decision "抽離 `_RemoteSkillBase` 為共用基底"; honours Decision "HuggingFace import 方向延 v0.3.4"] 新增 `libs/cantus/docs/protocols/adapters-batch2.md`：6 段結構（套件總覽連結到 v0.3.2 `adapters.md` 共用 convention；`expose_as_langchain_tool` + `import_langchain_tool` 5-line 範例（含 schema 轉換 note）；`expose_as_dspy_tool` + `import_dspy_tool` 5-line 範例（含 type mapping 表）；`expose_as_hf_tool` 5-line 範例 + HF import 方向延 v0.3.4 註記；`expose_as_openhands_action` 5-line 範例 + OpenHands action 子類選擇 note；`_RemoteSkillBase` 共用設計段（解釋 closure-based dispatch 三個對齊點，供 batch3 作者參考））。Verification：human review 確認 6 段皆有可貼上即跑的 Python 範例。

- [x] 8.2 [honours Decision "HuggingFace import 方向延 v0.3.4"] 新增 `libs/cantus/MIGRATION_v0.3_to_v0.3.3.md`：含 4 段：（a）LangChain 採用引導（雙向，含 Pydantic v2 args_schema 注意事項）；（b）DSPy 採用引導（雙向，含 type mapping note）；（c）HuggingFace 採用引導（export-only，含 HF Agent tools=[...] 接法）；（d）OpenHands 採用引導（export-only，含 cantus Skill → OpenHands action 在 OpenHands runtime 內怎麼 dispatch 的 host-code pattern）。Verification：human review 確認 4 段皆有可貼上即跑的 Python 範例片段。

## 9. 整合 verification（apply 完成 gate）

- [x] 9.1 `uv run pytest libs/cantus/tests/ -v` 整體綠（v0.3.0 + v0.3.1 + v0.3.2 + v0.3.3 聯合通過）。
- [x] 9.2 `uv run ruff check libs/cantus/` 與 `uv run mypy libs/cantus/cantus/` 兩者皆零錯誤。
- [x] 9.3 `uv run pytest libs/cantus/tests/test_skill.py::test_spec_for_llm_shape_unchanged libs/cantus/tests/test_skill.py::test_spec_for_llm_shape_unchanged_with_hooks libs/cantus/tests/adapters/test_skill_spec_for_llm_invariant.py -v` 全綠（v0.3.0 既有 contract 不破 + adapter import 雙層保險擴張到 9 個模組仍守住）。
- [x] 9.4 `spectra validate cantus-adapter-layer-batch2` 與 `spectra analyze cantus-adapter-layer-batch2` 兩條命令皆無 Critical / Warning（Suggestion 可保留）。
